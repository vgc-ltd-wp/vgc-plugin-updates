# VGC Stock Manager — Project Reference

> **Purpose of this file.** A complete, self-contained technical reference for the VGC Stock Manager system. Written so that a new chat (or a context-collapsed one) can pick up the work with no other background. Kept in GitHub (`vgc-ltd-wp/vgc-plugin-updates` → `docs/`), deliberately **not** part of any release zip.
>
> **Pinned to:** Stock Manager **1.75.1** · Stock Bridge **0.4.0**
>
> ⚠️ **This file is updated and pushed with every release** — it must never lag the shipped version. See §7 (Working conventions).

---

## 1. What this is

A **workshop inventory and production system** for a small manufacturer (candles, mugs, gift sets). It is **not** a shop — it runs on its own dedicated WordPress site (a backoffice subdomain) and pushes finished-goods stock to a separate WooCommerce shop.

### Two plugins

| Plugin | Slug | Installed on | Job |
|---|---|---|---|
| **VGC Stock Manager** | `vgc-stock-manager` | Backoffice WP site | The whole app: items, recipes, production, costing, reports, PWA |
| **VGC Stock Bridge** | `vgc-stock-bridge` | The WooCommerce shop | Tiny token-auth REST surface: list products, read/adjust/set stock. **No orders, no customers.** |

### Non-negotiable design decisions (do not "fix" these)

1. **Unified item model.** No hard line between material and product. A Base Candle is a sellable product *and* a component of a Gift Box. This is what makes nested recipes possible.
2. **The ledger is truth.** Stock is the **sum of append-only movements**, never a typed number. `items.stock_qty` is only a cache, rebuildable via `recompute_stock()`.
3. **Production is batch-based.** Recipes have a **yield** ("1 run makes 24"). The UI inputs **runs**, not units.
4. **Consume-from-stock.** One run = **one assembly level**. To make a Gift Box you must already have Base Candles in stock. You produce each level as its own run.
5. **Push deltas, never absolutes.** Producing a sellable item pushes `+N` to the shop, so **online sales made in the meantime are never clobbered**. `set` exists only as an explicit, confirmed reconcile.
6. **Shop price is never imported as cost.** A selling price is not a cost price; it would poison every roll-up and margin.
7. **All UI on the WP front-end**, at the **site root**. Never wp-admin. Other front-end URLs redirect to `/`.
8. **No framework, no build step.** Vanilla JS, `innerHTML` rendering, hash routing, one plain CSS file. Must work offline as an installed PWA — **no CDNs, no external fonts/icons**.

---

## 2. Repository / file layout

Local working dir: `C:\Projecs\Claude Code\`

```
vgc-stock-manager/                  ← the app plugin (zipped for release)
  vgc-stock-manager.php             main file: constants, table helpers, bootstrap
  uninstall.php                     drops tables + options
  readme.txt                        changelog
  includes/
    class-vgc-plugin-updater.php    shared VGC self-update (reused verbatim)
    class-install.php               dbDelta schema
    class-units.php                 VGC_SM_Units — unit registry + conversion
    class-repository.php            VGC_SM_Repository — all DB access + ledger
    class-bom.php                   VGC_SM_BOM — recipe maths
    class-production.php            VGC_SM_Production — production runs
    class-sync-client.php           VGC_SM_Sync — talks to the Bridge; settings
    class-i18n.php                  VGC_SM_I18n — catalogue + editable overrides
    class-help.php                  VGC_SM_Help — in-app wiki content
    class-access.php                VGC_SM_Access — capability + role
    class-rest-api.php              VGC_SM_REST_API — every endpoint
    class-frontend.php              VGC_SM_Frontend — shell, login, SW, manifest
  app/
    app.css                         the entire design system
    app.js                          the entire SPA
    icon-192.png, icon-512.png      PWA icons (generated, brand terracotta)

vgc-stock-bridge/                   ← the shop companion (zipped for release)
  vgc-stock-bridge.php
  includes/class-rest-controller.php   ← the whole surface
  includes/class-settings.php          ← token + API URL display
  includes/class-vgc-plugin-updater.php

_releases/                          ← built zips live here
vgc-stock-manager-design-brief.md   ← design brief (not shipped)
vgc-stock-manager-reference.md      ← THIS FILE (not shipped)
```

---

## 3. Data model

### Tables (prefix `wp_vgc_sm_`)

| Table | Purpose |
|---|---|
| `items` | The unified item model. |
| `categories` | User-defined categories. |
| `boms` | One recipe per manufactured item: `output_item_id`, `yield_qty`. |
| `bom_items` | Recipe components: `component_item_id`, `quantity`, `unit`, **`basis`** (`item`\|`run`). |
| `bom_extras` | Non-material costs: `label`, `amount`, `basis`. |
| `movements` | **Append-only ledger.** `type` (receive/consume/produce/ship/scrap/correct/consign_out/consign_return), signed `qty`, `ref_type`/`ref_id`, `partner_id`. |
| `production_runs` | One row per run; movements reference it. |
| `sync_log` | Every Bridge call. |
| `partners` | Shops we supply / makers we take from. `type` (`customer`\|`supplier`\|`both`), `active` (archived, never deleted), `vat_registered`/`vat_number` (1.9.0), `payment_terms_days` (1.10.0, default 30; drives note due dates). Flat `contact_name`/`email`/`phone`/`address` are a **cached mirror of the primary** child rows (kept in step by `sync_primary_fields()`). |
| `partner_locations` | Branches/warehouses, **each with its own contact**: `label`, `address`, `contact_name`/`contact_role`/`contact_email`/`contact_phone` (1.6.0), `is_primary`, `sort_order`. |
| `partner_contacts` | **Additional** people not tied to a location: `name`, `role`, `email`, `phone`, `is_primary`, `sort_order`. |
| `partner_prices` | Agreed unit price per (partner, item). Drives note pricing. |
| `stock_notes` | The documents. `number` (`SN-YYYY-NNNN`), `partner_id`, `location_id` (destination), `direction`, `type`, `status`, `note_date` (user-set; drives `issued_at`), `due_date` (note_date + partner terms; money notes only), `skip_stock` ("already sent" — no stock effect), `is_refund` (a return that credits money and skips the pile), `total_net`/`total_vat`/`total_gross`. |
| `stock_note_lines` | `item_id`, `qty`, `unit`, `unit_price` (**net**), `vat_rate`, `line_net`/`line_vat`/`line_gross`, `line_note`, `is_display` (a display piece on loan). |
| `consignment_ledger` | **Second append-only ledger**: how much of an item is at a partner. Releases add, sale reports and returns subtract; cancels write reversing rows. `is_display` splits the pile into **sellable vs display pools** — a sale report can only draw on the sellable pool. |
| `orders` | **The order model (1.11.0) — deliberately MUTABLE.** One living doc per customer order: `number` (`ORD-YYYY-NNNN`), `partner_id`, `location_id`, `date_ordered`/`date_sent`/`date_received`, `payment_mode` (`full`\|`on_sale`), `payment_terms_days`, `due_date`, `prices_include_vat` (1.12.0 — whether a typed `unit_price` already has VAT in it), `transport_cost`/`transport_payer` (`us`\|`customer`), `already_sent`, `paid_amount`, `status` (`open`\|`completed` — never final). |
| `order_lines` | `item_id`, `qty_ordered`/`qty_sent`/`qty_sold`/`qty_returned`, `unit_price`, `unit_cost` (**snapshot**, so margin never drifts), `vat_rate`, `is_display`, `line_note`, plus **`applied_out`/`applied_pile`** — what the ledgers already reflect for this line. |
| `order_payments` | Partial payments against an order (1.13.0): `paid_on`, `amount`, `note`. **Paid is SUM(payments)** — never a typed-over field. One-time `migrate_paid_amounts()` turns a legacy `orders.paid_amount` into the first payment (`vgc_sm_order_payments_migrated`). |
| `audit` | Activity log (1.7.0): `user_id`, `user_name`, `action` (dotted slug), `object_type`/`object_id`, `summary`, `ip`, `created_at`. Append-only, best-effort (never blocks the action it records). |

### `items` columns worth knowing

`sku` (unique) · `name` · `kind` (`raw`\|`manufactured`) · `unit` (base/stock unit) · `stock_qty` (**cache**) · `reorder_level` · `is_sellable` · `woo_sku` · `woo_product_id` · `supplier` · `barcode` · `category_id` · `image_id` · `pack_label` · `pack_size` · `cost_net` · `vat_rate` (default 20) · `active` (0 = **archived**) · `shop_held` (held units currently listed on the shop) · `shop_baseline` (shop qty as last accounted for, drives reconcile)

**DB_VERSION is currently `0.16.0`.** Bump it in `vgc-stock-manager.php` whenever the schema changes — `create_tables()` runs `dbDelta` on `plugins_loaded` when it differs, which auto-migrates.

### Options

| Option | Contents |
|---|---|
| `vgc_sm_db_version` | Schema version guard. |
| `vgc_sm_partners_split` | Set once the 1.3.0 flat→child partner migration has run. |
| `vgc_sm_notes_vat_backfill` | Set once the 1.9.0 backfill (pre-VAT notes → `total_gross = total_net`, 0% VAT) has run. |
| `vgc_sm_settings` | `bridge_url`, `bridge_token`, `auto_push`, `currency` (default `€`), `language` (default `en`). |
| `vgc_sm_units` | Custom (non-builtin) unit codes. |
| `vgc_sm_i18n_overrides` | `{ lang: { source_string: translation } }` — user-edited wording. |

---

## 4. The engine (the part that must stay correct)

### Units — `VGC_SM_Units`
Built-in registry with a **dimension** and a **factor**: weight (mg/g/dag/kg → base g), volume (ml/cl/dl/l → base ml), count (pcs). Custom units are count/1. `convert($qty,$from,$to)` scales within a dimension and returns unchanged across dimensions (degrade safely).

### Recipe maths — `VGC_SM_BOM`
The pivotal helper:

```php
per_item_qty($c, $yield)      // basis 'item' → quantity; basis 'run' → quantity / yield
demand_per_item($c, $yield)   // per_item_qty converted into the COMPONENT'S base/stock unit
```

**Everything routes through `demand_per_item()`** — `direct_requirements()`, `max_buildable()`, `plan_produce()`, `explode_raw()`, `unit_cost_net()`. This is why a recipe in grams reconciles with stock in kg, and why per-item and per-batch both work.

- `direct_requirements($item,$qty)` — one assembly level. Returns need (in the component's stock unit), available, short, unit_cost, line_cost, plus `recipe_unit`/`need_recipe` for the "180 g → 0.18 kg" display.
- `plan_build($item,$qty)` — recursive **net requirements**: uses on-hand stock of intermediates first, only builds/buys the shortfall. A mutable `available` map is decremented as it allocates, so a component shared across branches isn't double-counted. Returns `feasible`, ordered `builds` (children first), `raw_short`.
- `unit_cost_net($item)` — recursive roll-up: Σ(component unit cost × demand_per_item) **+ extras** (per-item, or per-batch ÷ yield). Cycle- and depth-guarded.
- `would_create_cycle()` — used by the recipe editor to reject A→B→A.

### Production — `VGC_SM_Production`
`run()` is **transactional**: `production_runs` row + `consume` movements + `produce` movement + optional `scrap` movement, all inside `START TRANSACTION`/`COMMIT`. Blocks on shortages unless `allow_negative`. Fires:

```php
do_action( 'vgc_sm_production_completed', $item_id, $net_qty, $run_id );
```

`$net_qty` is **produced minus scrap** — the sync client listens here and pushes that, so defective units never reach the shop.

### Sync — `VGC_SM_Sync`
`request()` → Bridge with `X-VGC-Token`. `push_adjust()` (delta, the normal path), `push_set()` (reconcile only), `read_stock()`, `pull_products()`, `shop_levels()`, `push_item()`. Everything logged to `sync_log`.

### Partners and stock notes — `VGC_SM_Partners`, `VGC_SM_Notes`

`VGC_SM_Notes::types()` is the single table that drives everything. Each type declares its effect:

| type | direction | `stock` | `consignment` | movement | owes us | we owe |
|---|---|---|---|---|---|---|
| `release` | out | −1 | +1 | `consign_out` | no | no |
| `sale_report` | out | 0 | −1 | — | **yes** | no |
| `return_in` | out | +1 | −1 | `consign_return` | no | no |
| `direct_sale` | out | −1 | 0 | `ship` | **yes** | no |
| `take_in` | in | 0 | +1 | — | no | no |
| `purchase_in` | in | +1 | 0 | `receive` | no | **yes** |
| `buy_held` | in | +1 | −1 | `receive` | no | **yes** |
| `return_out` | in | 0 | −1 | — | no | no |
| `sold_held` | in | 0 | −1 | — | no | **yes** |

Read it as: *what happens to my stock* and *what happens to the pile*. For `direction=out` the pile is **our goods at the partner** (`consignment_ledger` direction `out`); for `direction=in` it is the **held bucket** — the maker's goods at our place (direction `in`). A release moves goods out of stock into the out-pile without selling; a sale report never touches stock but clears the out-pile and the partner owes us. Inbound mirrors it: `take_in` fills the held bucket without touching our stock; `sold_held`/`buy_held` empty it and we owe the maker; `buy_held`/`purchase_in` are the only inbound types that add to our own stock.

**Held stock is never our own stock and is never pushed to the shop** (that is the on-demand Phase 4). Only `stock`-affecting movements push, so `purchase_in`/`buy_held` push a positive delta for sellable items; everything else inbound is stock-neutral.

- `save_draft()` — drafts only; an issued note is immutable. A line with no price falls back to the partner's price list.
- `issue()` — **pre-flight first**: never release stock you do not have, never report/return more than the partner is holding. Problems → `WP_REST_Response` 409 with `problems[]` and **nothing is written**. Otherwise one transaction: movements + consignment rows + number + status, then shop pushes.
- `cancel()` — writes **reversing entries** in both ledgers and pushes the reverse to the shop. Never deletes.
- `outstanding()` / `outstanding_all($partner,$direction)` / `at_partners()` / `held()` / `balance_owed()` / `balance_we_owe()` — all derived by SUM over the ledgers, so they cannot drift. `outstanding_all(..,'in')` is the held report; `held($item)` is the per-item held total.
- `statement($partner,$from,$to)` (1.5.0) — every money-bearing note both ways over a period (issued + settled), with outstanding `they_owe` / `we_owe` / `net`. Outbound money = `sale_report`,`direct_sale`; inbound money = `purchase_in`,`buy_held`,`sold_held`. Only `issued` notes count towards outstanding.
- `push_to_shop()` — sellable + `auto_push` only, and always a **delta**.

**Shop publishing of held stock (1.4.0).** Held goods stay off the shop until the operator publishes them.
- `publish_held($item,$qty)` — push `+qty` delta, `shop_held += qty`, set `shop_baseline` to the shop qty just read back. `unpublish_held()` is the reverse.
- `reconcile_scan()` — for every item with `shop_held > 0`, read the shop; `sold = clamp(baseline − current, 0, shop_held)` (only the held share; own-stock sales are out of scope by the push-only design); split `sold` across makers **FIFO by earliest `created_at`** via `held_by_maker()`.
- `reconcile_preview()` (advisory) / `reconcile_apply()` (books one issued `sold_held` note per maker with `skip_clamp`, then lowers `shop_held` and re-baselines). Both recompute live.
- **Invariant:** the shop never lists more of a maker's goods than we still hold. `clamp_shop_held($item)` runs after any held-reducing note (`issue()` for inbound `consignment<0`; `cancel()` of a `take_in`) and pushes the surplus off. Reconcile passes `skip_clamp` because the online sale is what reduced the shop.

### Orders — `VGC_SM_Orders` (1.11.0)

The user asked for a spreadsheet, not a stack of immutable documents. So an order is **fully editable, forever** — and the ledgers stay append-only anyway, via reconciliation rather than rewriting.

- **The invariant:** every line stores `applied_out` / `applied_pile` = what the movement ledger and the consignment ledger already reflect for it. On each `save()` → `reconcile()`, the engine computes the *desired* state and writes **only the delta**:
  - `want_out  = already_sent ? 0 : (qty_sent - qty_returned)` → movement of `-(want_out - applied_out)`
  - `want_pile = qty_sent - qty_sold - qty_returned` → consignment row of `want_pile - applied_pile`
  This is why editing a sent qty from 3 → 5 a week later Just Works, and why history is never rewritten.
- **Removing a line** zeroes its quantities first so reconcile releases the stock, *then* deletes the row (same for `delete()` of a whole order).
- `net_unit($line, $incl)` (1.12.0) is the one place VAT-inclusive entry lives. `unit_price` is stored **exactly as typed**; `prices_include_vat` says how to read it (`incl ? price/(1+vat/100) : price`). Storing the typed figure rather than a converted one is deliberate — no rounding drift.
- **Display lines carry no money at all**: `billable_qty()` returns 0 for them, and the editor renders them in a separate section on a shorter grid (`.vgc-sm-oline--demo`, 8 columns) with no price/cost/VAT/margin fields.
- `billable_qty()` is the one place the payment mode lives: `full` → `sent - returned`; `on_sale` → `sold`. Display lines are **never** billable.
- `totals()` derives everything (net/VAT/gross/cost/**margin**/paid/balance/out_there). Transport lands on whoever `transport_payer` says: on the customer it adds to gross; on us it comes out of margin.
- `set_complete()` only flips `status`. **No status is final** — this is a product requirement, not an oversight.
- Orders write to the **same** ledgers as stock notes, so "Out on consignment", item stock and the shop push all keep working across both models.

---

## 5. REST API (`vgc-stock/v1`)

Auth: same-origin cookie + `X-WP-Nonce`. **Access levels (1.7.0)** — `VGC_SM_Access::level()` resolves a user to `viewer`|`operator`|`manager`|`admin`: admin = `manage_options`; otherwise the `vgc_sm_level` user-meta (default `operator`) for anyone with the `vgc_sm_access` cap; `null` = no access. Permission callbacks: `$auth` = viewer+ (reads), `$write` = `require_operator` (day-to-day writes), `$mgr` = `require_manager` (partners/prices/deletes/shop ops), `$admin` = `authorize_admin` (settings/translations/team/audit). Enforcement is server-side; the SPA hides what a level can't do via boot `perms` (`write`/`manage`/`admin`). Existing Stock Operators resolve to `operator`; nothing to migrate.

| Method | Route | Notes |
|---|---|---|
| GET/POST | `/items` | list (filters: `search`, `kind`, `category_id`, `low_only`, `archived`) / create |
| GET/PUT/**DELETE** | `/items/{id}` | DELETE takes `?mode=archive\|delete`; refuses if used in a recipe (409 + `used_in`) |
| GET | `/items/{id}/movements` | ledger history |
| GET | `/items/{id}/shop-stock` | live shop qty for a sellable item |
| GET | `/items/lookup?code=` | barcode/SKU |
| GET | `/items/suggest-sku?name=` | unique SKU from a name |
| POST | `/items/import` | CSV rows upsert |
| POST | `/movements` | `receive` \| `ship` \| `scrap` \| `adjust` \| `set` |
| GET/PUT | `/recipe/{id}` | components + extras; PUT rejects cycles |
| GET | `/production/preview` | requirements, shortages, materials/extras/total cost |
| GET | `/production/plan` | recursive build plan |
| POST | `/production/run` | `qty`, `allow_negative`, `scrap_qty`, `scrap_note` |
| GET | `/dashboard` | low materials / low products |
| GET | `/reports/stock`, `/reports/production` | + scrap; CSV built client-side |
| GET/POST | `/categories`, PUT/DELETE `/categories/{id}` | |
| GET/POST | `/units` | codes + `meta{dimension,factor}` |
| POST | `/media` | image upload (gated by `vgc_sm_access`, **not** `upload_files`) |
| GET/POST | `/settings`, POST `/settings/test` | *(admin)* |
| GET/POST | `/translations`, POST `/translations/reset` | *(admin)* |
| GET | `/shop/levels`, POST `/shop/push` | |
| GET | `/shop/products`, POST `/shop/pull` | *(admin)* pull products, incl. image sideload |
| GET/POST | `/partners`, GET/PUT/**DELETE** `/partners/{id}` | DELETE archives. GET {id} returns outstanding + notes + price list + `owed`. |
| POST | `/partners/{id}/prices` | upsert one agreed price (empty `unit_price` clears it) |
| GET | `/partners/{id}/statement?from=&to=` | money owed both ways over a period (lines + totals) |
| GET/POST | `/notes`, GET/PUT/DELETE `/notes/{id}` | PUT/DELETE are **drafts only** |
| POST | `/notes/{id}/issue` | 409 + `problems[]` if it would break stock |
| POST | `/notes/{id}/cancel`, `/notes/{id}/paid` | cancel = reversing entries; paid = `settled` |
| GET | `/consignment/outstanding` | `direction=out` (default, "what have we shipped") or `direction=in` (held stock); + totals at price and at cost |
| POST | `/consignment/publish`, `/consignment/unpublish` | list/de-list an item's held units on the shop (`item_id`, `qty`; qty≤0 = all) |
| GET/POST | `/consignment/reconcile` | GET = preview; POST = book detected shop sales as `sold_held` notes |
| GET/POST | `/orders`, GET/PUT/DELETE `/orders/{id}` | the order model; PUT is always allowed (never locks) |
| POST | `/orders/{id}/complete` | `{complete:0\|1}` — reversible |
| GET/POST | `/locations`, PUT `/locations/{id}` | sale locations (POS). Create/edit = *(manager)*. |
| GET | `/locations/{id}/inventory` | a location's on-hand rows (qty, retail, value) |
| POST | `/locations/{id}/push`, `/locations/{id}/pull` | *(manager)* hard-transfer stock workshop↔location (`item_id`, `qty`) |
| GET | `/items/{id}/locations` | where an item is stocked across locations |
| GET/POST | `/sales` | till sales — list (filters: `location_id`,`from`,`to`; returns `rows`+`totals`) / checkout (`location_id`,`lines[]`,`discount_type`,`discount_value`) |
| GET | `/sales/{id}` | one receipt (lines + net/VAT/total + refund state) |
| POST | `/sales/{id}/refund` | *(manager)* refund lines (`lines:[{line_id,qty}]`) or `void:true` — returns stock to the location |
| GET | `/help` | the in-app wiki |
| GET/POST | `/team`, `/team/{id}` | *(admin)* list users + levels / set a user's level (`viewer`\|`operator`\|`manager`\|`none`) |
| GET | `/audit` | *(admin)* activity log, filtered (`search`,`user_id`,`action`,`from`,`to`,`page`) + paginated |

### Bridge (`vgc-stock-bridge/v1`) — on the shop
Auth: `X-VGC-Token` header (shared secret) over HTTPS.

`GET /ping` · `GET /products?page&per_page&search&category&type` (variations expanded; returns `image`+`image_full`, `virtual`/`downloadable`; `category`=term slug, `type`=simple/variable/grouped/external — 0.4.0) · `GET /product-categories` (0.4.0) · `GET /stock?skus=` · `POST /stock/adjust` (**signed deltas — the normal path**) · `POST /stock/set` (absolute).

---

## 6. Front end

`class-frontend.php` serves the app at the **site root** on `template_redirect` with a standalone full-screen template (no theme). Not logged in / lacking `vgc_sm_access` → a branded login (not `wp-login.php`).

- **PWA**: manifest + service worker served *virtually* at `/vgc-app.webmanifest` and `/vgc-sw.js` (matched by path on `init`, so root scope works). SW is registered with `updateViaCache:'none'` + a version-stamped URL, calls `reg.update()` each load and **reloads once on `controllerchange`** — this is why plugin updates land without a hard refresh. The REST API is **never** cached.
- **Offline**: writes go through an **IndexedDB queue** (`write()`), replayed on reconnect. Reads fail offline by design.
- `app.js` boot payload (`window.VGC_SM`): `restUrl`, `nonce`, `appUrl`, `swUrl`, `logoutUrl`, `canManage`, `currency`, `lang`, `strings`, `user`, `version`.

**POS till — separate page.** `maybe_render()` branches on `?vgc_sm_pos=1` (needs `operator`) → `render_pos()` serves its own full-screen shell that loads `core.js` (helpers only, no `VGCSM.start()`) + `js/pos/pos.tpl.js` + `js/pos/pos.js`, then `window.VGCSM_POS.start()`. Opened from the **Open till** button on Sale locations (`posUrl` in the boot). A **Scan** button opens a self-contained camera overlay (BarcodeDetector, mirrors `production.js`'s scanner but continuous with a 1.5s same-code de-dup); each scan calls `GET /items/lookup?code=` then adds from the location's stock via `addToCart` (which caps at on-hand). Camera-absent/insecure-context falls back to manual barcode/SKU entry. Data model: `sale_locations` + `location_ledger` (truth) + `location_stock` (cache, PK location+item); `sales` + `sale_lines`. Push/pull = **hard transfer** (`record_movement(item,'to_location',∓qty)` + location ledger); a sale is a **location-only decrement** (no stock movement) that records a receipt. Company inventory = `stock_qty` + Σ `location_stock`. Discount math is gross-facing: per-line (`pct`|`val`) → line_gross; cart discount allocated across lines by gross share (last line takes the rounding remainder); net = gross/(1+vat/100). `VGC_SM_Locations` + `VGC_SM_Sales`; REST in `trait-pos.php`.

**Refund / void (1.66.0, DB 0.23.0).** `VGC_SM_Sales::refund($sale_id, $lines, $note, $void)` returns units to the selling location via `VGC_SM_Locations::return_from_sale` (a `sale_return` location-ledger entry, mirror of `deduct_for_sale`; no stock movement). Per-line refund value is proportional to the stored post-discount line totals; **clearing a line takes the exact stored remainder**, so a full void returns precisely what was charged. New columns: `sale_lines.refunded_{qty,net,vat,gross}`, `sales.refunded_{net,vat,gross}` + `refunded_at`/`refunded_by`; `sales.status` moves `completed`→`partial_refund`→`voided`. `recent()` surfaces `status`, `refunded_gross`, `net_gross`. Manager-only, on the main-app receipt (`receipt-refund` card) — the till stays sell-only. Defensive `ensure_refund_columns()` (guarded by option `vgc_sm_sales_refund_columns`) adds the columns if dbDelta skipped them.

**Printable receipt (1.67.0).** The receipt page has **Print 80mm slip** / **Print A4** buttons → `openReceiptPrint(mode, sale)` mounts the shared `.vgc-sm-printov` overlay (same mechanism as the order print sheet) with `V.tpl.locations.receiptSheet` (single-column `.vgc-sm-rsheet`, `.is-80` = compact thermal slip). Paper size is set per mode by injecting a `<style id="vgc-sm-pagesize">@page{size:80mm auto|A4}</style>` at print time (removed on close). Letterhead comes from `boot.print` (Settings). No server side — pure print/PDF.

**"Nothing here yet" on every location — the real root cause (1.71.0).** `VGC_SM_Locations::inventory()` and `shape()` selected **`i.price_gross`**, but there is **no such column** on the items table — `price_gross` is derived (`price_net * (1 + vat_rate/100)`) everywhere in the app. MySQL threw *Unknown column* → `get_results` returned empty → location inventory, the till product grid, and location value totals were all blank, even though the item page's `item_breakdown()` (which selects no item price columns) showed the right per-location quantities. Fixed by selecting `price_net` + `vat_rate` and computing gross in PHP (`inventory`/`shape_stock_row`/`shape`, and `VGC_SM_Sales::checkout` which read `$item['price_gross']` off the raw `SELECT *` row — also undefined). **Lesson:** the items table stores `price_net`/`price_b2b`/`cost_net`/`vat_rate` only; never `SELECT i.price_gross`/`i.cost_gross`.

**Location-stock cache is ledger-derived (1.70.0).** `location_stock` is **recomputed from `location_ledger`** after every write via `VGC_SM_Locations::sync_stock_cache($loc,$item)` (SELECT SUM → upsert as a `%s` canonical-decimal string), so it can't drift. One-time repair on upgrade (option `vgc_sm_pos_tables_repaired_v1`): `VGC_SM_Install::ensure_pos_tables()` (`CREATE TABLE IF NOT EXISTS`) + `VGC_SM_Locations::rebuild_stock_cache()`. Good hygiene, but note the cache was NOT the display bug — the phantom-column SELECT above was. Push/pull UI is Orders-style inline (staged rows / inline pull editor) — **no `prompt()`** (see [[vgc-stock-manager-no-browser-prompts]]).

**Till redesign (1.75.0, DB 0.25.0) — first slice of the Claude Design pass.** Design deliverable lives in the `App design brief` project (`0aea936f-…`, read via the DesignSync MCP) and locally as `vgc-stock-manager-design-v3.html`; **tokens are unchanged**, so the whole redesign is CSS + markup inside the existing system. Till changes: dark counter bar (`--vgc-panel`), location picked from a **sheet** (`.vgc-sm-possheet`) not a `<select>`, **filter chips** (All / ★ Favourites / one per category, derived from what's actually on the shelf), tiles with image-or-tinted-placeholder + in-cart badge + disabled-when-out, `%`/amount **segmented** discounts, green **Charge** button (`--vgc-ok`), and a done screen that can **print the 80mm slip from the counter**. Phone: the sale becomes a **docked summary** (`.vgc-sm-posdock`, mini lines + total always visible) that expands into a bottom **sheet** — the answer to "you can't see the total while adding". New data it needs: `items.favourite`, `sale_locations.kind` (counter/popup/market/other), and `category`/`favourite` now returned per location-inventory row (LEFT JOIN categories). Guarded by `vgc_sm_item_columns_v4` + `VGC_SM_Locations::ensure_location_columns()` (`ensure_pos_tables()` only creates missing tables — it cannot ALTER an existing one). The receipt sheet moved to **`js/tpl/receipt.tpl.js`, loaded by both the app and the till page** (`V.tpl.receipt`; `V.tpl.locations.receiptSheet` now delegates), and the till boot gained `print` (letterhead). The till page is **viewport-fit=cover + black-translucent**, so every till surface must inset itself with `env(safe-area-inset-*)` (1.75.1 — the bar grows by the top inset, the dock/foot/sheets clear the home indicator; `height:100dvh` because iOS 100vh includes browser chrome). The main app shell already did this; the till CSS did not. **Still to do from the design:** nav grouping (Catalogue/Trade/Sell/Stock/Admin + a 5-tab phone bar with a Menu tab), refund-in-place on the receipt, location list/detail tiles, a Sales-over-time chart.

**QR bin labels (1.73.0, DB 0.24.0).** `app/js/qr.js` is a **self-written** QR encoder (byte mode, EC level M, versions 1–6 ⇒ 106-char ceiling; auto version + mask). Written rather than vendored because the app is a self-hosted offline PWA. **Verified by round-trip decoding with `zbar`** — 7 payloads × 8 forced masks + auto — not by matrix-diffing a reference encoder (that gives false failures: `segno` picks alphanumeric mode and pads differently). That round-trip caught a real bug: the alignment-pattern centre is `size-7`, **not** `4*version+6`; v1 decoded fine while every v≥2 was unreadable, i.e. every real label URL. `V.qrSvg(text, {size, unit, quiet, fg, bg})` renders inline SVG. Label = `item-label` card, 65×55mm (`.vgc-sm-lbl`), QR at 32mm with a **4-module quiet zone**, always on a **white tile** so a dark `label_color` stays scannable; ink auto-flips black/white by luminance. `label_color` is a nullable `#rrggbb` column on items (strict regex in `sanitize()`, which is a **whitelist** — a new field silently vanishes unless added there), added via dbDelta + `ensure_item_columns()` guarded by the new `vgc_sm_item_columns_v3` option (v2 is already set on live installs). Print uses the shared overlay with `@page{size:65mm 55mm;margin:0}`; Download SVG emits the QR at exact size. QR payload = `<appUrl>?vgc_sm_scan=<SKU>`.

**Scan route + landing (1.74.0).** `maybe_render()` branches on `?vgc_sm_scan=<code>` **after** the auth gate → `VGC_SM_Repository::get_item_by_barcode()` (barcode, falling back to SKU — which is why a SKU payload resolves) → `wp_safe_redirect( url() . '#/item/{id}?scanned=1' )`; no match → `#/products?scan_unknown=<code>`, which the router turns into a warn toast. The login form's action carries `vgc_sm_scan` through, so scanning while signed out still lands on the item after login. The router passes `r.query` into `S.viewItem(id, query)`; `scanned` renders `scannedPanel()` (`item-scanned`) **above the hero** with **Receive stock** → the existing inline `movementForm(i,'receive')` and **Record production** → `#/produce?item={id}` (made items only). This is the whole point of the QR work: it needs **no in-page scanning API**, so it works on iOS where `BarcodeDetector` does not.

**Mobile navigation (1.72.0).** The bottom tab bar only renders `NAV_MAIN` entries that carry a `tab:` label (6 of them), so every other destination used to be desktop-sidebar-only — unreachable on a phone. The topbar now has `#vgc-sm-menu` (`.vgc-sm-topbar__menu.vgc-sm-phoneonly`, hidden ≥900px) → `openNavDrawer()` in core.js, which reuses `openDrawer()` to list **all** groups (`NAV_MAIN` + `NAV_MANAGER` if manager + `NAV_ADMIN` if admin + `NAV_HELP`) with the active route highlighted, plus the signed-in user and sign-out in the drawer foot. Each entry carries `data-drawer-close="1"` so a tap navigates *and* dismisses. The click is bound at **module load** (not in `V.start()`) so the menu works as soon as the shell exists.

### Design system (0.16.0, implemented from a Claude Design project)
Warm parchment + terracotta, "Workshop backoffice".
Brand `#B65C22` · bg `#F5F1EA` · card `#FFF` · ink `#221F1A` · danger `#B42318` · ok `#2E7048` · dark payoff panel `#221F1A`.
Inline SVG icon set (`ICON_PATHS` / `icon()`). **Desktop ≥900px** = sidebar + tables + sticky cost panel; **phone** = bottom tabs + FAB. Numbers/SKUs/money are monospace + tabular.

### i18n (self-contained — the SPA has no `wp.i18n`)
English source strings are the **keys**. `VGC_SM_I18n::map()` = shipped catalogue **+ user overrides on top**. In JS: `t()` for composed/dynamic strings, toasts, prompts; plus `translateTree()` (a TreeWalker) that translates whole static text nodes and `placeholder`/`title`/`aria-label` after each render. **Changing language reloads the page** (the catalogue ships in the boot payload).
To add a language: add a catalogue method in `class-i18n.php` and list it in `languages()`.

---

## 7. Working conventions (follow these)

1. **Bump the version on every change** — plugin header + `VGC_STOCK_MANAGER_VERSION` constant + `readme.txt` Stable tag + changelog entry.
2. **Build zips with forward slashes.** PowerShell's `Compress-Archive` writes backslash entry paths that **break on Linux**. Use `System.IO.Compression.ZipArchive` and `CreateEntry(rel.Replace('\\','/'))`. Zips go to `_releases/` as `vgc-<slug>-<version>.zip`.
3. **The user tests on staging** — do not run or lint the plugin locally. (`node --check app/app.js` and a CSS brace check are fine and worth doing.)
4. **TEST MODE: publish every update immediately** (until the user says production mode). Publishing =
   - `gh release create vgc-<slug>-<version> _releases/<zip> --repo vgc-ltd-wp/vgc-plugin-updates`
   - update `plugins.json` in that repo (`version`, `download_url`, prepend changelog)
   - **update THIS file** — the "Pinned to" line, the feature-history table, and any section the change touches — and copy it to `docs/vgc-stock-manager-reference.md` in the same commit
   - commit, push, verify with `gh api .../contents/plugins.json` (the raw CDN lags ~5 min).
5. **This reference is always pinned to the current version.** It is part of the release, not an afterthought: a release is not finished until the doc matches what shipped. A stale reference is worse than none, because the next session will trust it.
6. **Self-update wiring**: instantiate `VGC_Plugin_Updater` **after** the `ABSPATH` guard. (Several older VGC plugins have it *inside* the guard, so it never runs — a real bug worth auditing separately.)
7. Update the memory files under `.claude/projects/.../memory/` when a durable decision changes.

---

## 8. Feature history (what exists and why)

| Ver | Feature |
|---|---|
| 0.1–0.5 | Foundation, front-end shell + login, BOM engine, REST + SPA + PWA, sync client |
| 0.6 | Packaging (packs) + pricing with VAT |
| 0.7 | Costing on Produce; **production became run/batch-based** |
| 0.8 | Recipe components **per item or per batch** |
| 0.9 | **Unit conversion** (weight/volume) |
| 0.10 | Non-material costs (labour/overhead) |
| 0.11 | SKU generation, currency (€ default), price formatting |
| 0.12 | **Cache-safe updates**, Reports, CSV import, scrap |
| 0.13 | Scrap as part of a production run (net good pushed) |
| 0.14 | Categories |
| 0.15 | Item photos |
| 0.16 | **Design system implementation** (sidebar + tables + panels) |
| 0.17 | UI language setting + full Bulgarian |
| 0.18 | **Pull products from the shop** (Bridge 0.2.0) |
| 0.19 | Pull imports photos; shop stock on item detail (Bridge 0.3.0) |
| 0.20 | **Remove items**: archive vs permanent delete, recipe-usage guard |
| 0.21 | Full-width desktop; sortable items table |
| 0.22 | Sorting persists; sorting on reports/shop/pull |
| 0.23 | Sidebar category shortcuts (`#/items?category=…`) |
| 0.24 | **Editable translations** (overrides survive updates) |
| **1.0.0** | **In-app wiki**; first stable release |
| 1.0.1 | Shop stock: photos fixed (`/shop/levels` now returns `image_thumb` + category), category filter + sortable Category column; sidebar category shortcuts made collapsible (open only in the Items section) |
| 1.1.0 | **Partners + stock notes (outbound)**: price lists, `SN-YYYY-NNNN` documents (release / sale report / return / direct sale), draft → issue → settle/cancel, pre-flight validation, consignment ledger, **Out on consignment** report, "At partners" on item detail, shop reduced on release |
| 1.2.0 | **Inbound notes + held bucket**: take on consignment / purchase / buy-held / return-out / sold-from-held; `held()` + `balance_we_owe()`; partner page shows held goods + "You owe"; **Held stock** report; "Held (from makers)" on item detail; note-type dropdown grouped inbound/outbound. Also wired the `/outstanding` route that 1.1.0 shipped un-routed. |
| 1.3.0 | **Multi-location / multi-contact partners**: `partner_locations` + `partner_contacts` child tables (one primary each), repeatable rows in the partner editor, directory card on the partner page. Flat partner fields become a cached mirror of the primaries (`sync_primary_fields()`); one-time `migrate_flat_fields()` seeds child rows from the old columns (guarded by `vgc_sm_partners_split`). Partner create/update accept `locations[]`/`contacts[]`. |
| 1.4.0 | **Shop publishing of held stock** (Phase 4): `items.shop_held` + `shop_baseline`; publish/unpublish held units to the shop; "Reconcile from shop" reads the shop back, FIFO-splits online sales across makers, previews then books `sold_held` notes; `clamp_shop_held()` invariant keeps the shop from ever offering more than we hold. Item detail gains Publish/Unpublish + "On shop from held"; Held stock screen gains Reconcile. |
| 1.5.0 | **Partner statements + printable documents** (Phase 5): `VGC_SM_Notes::statement()`, `/partners/{id}/statement`; Statement screen (`#/statement/{id}`) with date range, both-way outstanding totals, net position, CSV. `@media print` letterhead (boot `siteName`) prints issued notes and statements as one clean sheet; `.vgc-sm-noprint`/`.vgc-sm-printonly`/`.vgc-sm-printhead` control what shows. **Completes the consignment feature set.** |
| 1.6.0 | **Per-location contacts**: `partner_locations` gains `contact_name/role/email/phone`; the standalone `partner_contacts` list is relabelled "Additional contacts". `sync_primary_fields()` now prefers the primary location's contact (falls back to the first additional contact). Partner editor location rows gain a contact block; directory card shows each location's contact. DB_VERSION 0.10.0 (dbDelta adds the columns; no data migration). |
| 1.7.2 | Partner name shown in the sticky top bar on the partner page (`setTopTitle()` in `viewPartner`). |
| 1.7.3 | Pull products fetched all pages at 100/page (superseded by 1.7.4). |
| 1.7.4 | **Pull products paginated** — `viewPull` loads one page of `PULL_PER=20` at a time with Previous/Next; selection persists across pages in a `picks` map keyed by `product_id`. |
| **1.63.0** | **POS Phase 2 — the till (separate page) + checkout.** No DB change (tables from 1.62.0). **`class-sales::checkout($location_id, $lines, $discount_type, $discount_value, $note)`:** validates location active + per-line on-hand (oversell → 400); prices are gross; per-line discount ('pct'|'val') → `line_gross`; subtotal = Σ line_gross; cart discount on subtotal → `total_gross = subtotal − cart_discount`; the cart discount is **allocated across lines by gross share, last line takes the rounding remainder** so Σ final_gross == total_gross exactly, then each line's net = gross/(1+vat/100), vat = gross−net (per-line exact → totals exact). Writes `sales` (number `S-%05d` set post-insert) + `sale_lines`, calls `VGC_SM_Locations::deduct_for_sale` (location-only, no stock movement — goods left workshop at push), audits. `get($id)` shapes the receipt. **REST** POST `/sales` ($write) → `create_sale`. **Separate till page:** `class-frontend::maybe_render()` branches on `?vgc_sm_pos=1` (operator+, else redirect) → `render_pos()`: own `<head>`+`<body class="vgc-sm--pos">` with `#vgc-sm-pos`+`#vgc-sm-toast`, boot (restUrl/nonce/currency/strings/lang/user/perms), loads `pos_scripts()` = core.js + `js/pos/pos.tpl.js` + `js/pos/pos.js`, then `window.VGCSM_POS.start()` — **reuses core.js helpers (get/rawFetch/money/t/toast/translateTree) WITHOUT `VGCSM.start()`** (no backoffice router/chrome/SW-register). `posUrl` added to render_app boot. **Till JS** (`window.VGCSM_POS`, standalone): loads active locations → inventory grid (on-hand>0, client search filter) → tap-to-cart (qty capped at on-hand) → cart lines (qty stepper, editable gross price, per-line discount control, remove) + cart discount + live net/VAT/gross (math mirrors the server) → checkout POST `/sales` → receipt card (number+totals) + "New sale", inventory refetched. **Launcher:** "Open till" `<a target=_blank>` (boot.posUrl) on the `locations-toolbar`. CSS `.vgc-sm-pos*` (bar/grid/tiles/lines/totals; single-column ≤899px). Harness (till shell, real math): shell (active-only location select, back-to-app, 2 tiles, empty cart), add-to-cart, subtotal 39/net 32.50/vat 6.50, oversell cap at 6 + toast, line 10%→21.60, cart 10%→discounts 3.90/net 29.25/vat 5.85/total 35.10 (hand-verified), checkout POST body correct→receipt→inventory refetch→New-sale resets, phone single-column no-h-scroll, console clean. Server `php -l` clean (checkout/DB verifiable on staging). **Phase 3 next:** sales history + inventory/value views + CSV. |
| **1.62.0** | **POS Phase 1 — Sale locations (foundation). DB_VERSION 0.22.0.** New feature: multi-location retail. **Model chosen (user):** generalize the single physical store into named sale locations; **hard transfer** on push (goods leave workshop stock the moment they're pushed — `stock_qty` drops, location holds them; a sale later empties only the location). Company inventory = `stock_qty` + Σ`location_stock`. **Tables (5):** `sale_locations`, `location_ledger` (truth) + `location_stock` (cache, PK location+item, upserted via INSERT…ON DUPLICATE KEY), `sales`+`sale_lines` (created now, used by the till in Phase 2). **`class-locations.php`:** CRUD; `push()` = `record_movement(item,'to_location',-qty)` + ledger +qty (clamped to workshop `stock_qty`); `pull()` = reverse; `deduct_for_sale()` (Phase 2, location-only, no stock movement); on_hand/inventory/item_breakdown; `shape()` aggregates lines/units/retail/cost. **`class-sales.php`** stub. **Migration** `migrate_store()` (option `vgc_sm_pos_store_migrated`): creates a default "<site> — Store" location, hard-transfers each item's `store_qty` into it (movement −store_qty so stock_qty drops, ledger +store_qty), zeroes store_qty — "at workshop"/"at store" preserved, conservation holds. **REST** `trait-pos.php`: GET/POST `/locations`, PUT `/locations/{id}`, GET `/locations/{id}/inventory`, POST `/locations/{id}/push|pull` (all $mgr except reads $auth), GET `/items/{id}/locations`. **Backoffice JS** new `locations` domain (tpl+logic): `#/locations` list + `#/location/{id}` detail (inventory table, push-search→prompt→POST, pull-per-row, edit form); NAV_MANAGER "Sale locations", TITLES/activeKey/routeTitle wired; `app_scripts()` +2 files. **Item page:** the old `item-store` distribute/sold-at-store card removed (superseded); replaced by a read-only `item-locations` breakdown fetched from `/items/{id}/locations`; catalogue.js viewItem now Promise.all's a 3rd fetch. Devids documented. **Verified (frontend harness, manager perms):** nav link, list (2 rows, inactive badge, retail value), create→`#/location/3`, detail header/inventory, push (search→workshop-avail→prompt→POST `/push`), pull (POST `/pull`), edit (prefill+active→PUT), item-locations card present & store card gone, 22-route regression, console clean. Server (tables/migration/REST) is `php -l` clean; DB migration verifiable only on a real WP/DB (staging). **Deferred to Phase 2:** the separate till page (`?vgc_sm_pos=1`, own shell reusing core helpers, no backoffice router) + `class-sales` checkout + "Open till" launcher. Phase 3: sales history + inventory/value views. |
| **1.61.1** | **Fix: i18n drawer source labels were localised.** Root cause (not a capture bug — capture was correct English): `openDrawer()` runs `translateTree()` over the drawer, which localises any text node matching a catalogue key — so each English `<code>` source label got translated into the current language (all tokens read Cyrillic in bg). Fix in `bindI18n()`: after the drawer renders, restore every `.vgc-sm-i18nrow__src` textContent from its raw `data-i18ncopy` token (attributes are untouched by translateTree). The editable input keeps the current translation (STR[k]); copy/edit/save/filter already keyed off the English source attribute, unaffected. Verified: bg drawer labels now English ("Materials"/"Archived"/"Administrator"/"All categories") with Bulgarian inputs, 0/20 Cyrillic labels, filter on English keys, save still POSTs `strings.<EnglishKey>` + live re-render, console clean. |
| **1.61.0** | **Dev-mode i18n token inspector (core.js only).** Captures the exact translation tokens rendered on a page and lets you fix wording inline. **Capture:** `t(s)` and `translateTree()` push `s` into `_i18nSeen` while `_i18nRec` is on; `_i18nRec` tied to dev mode in `applyDevMode()` (zero cost off); reset per navigation at the top of `router()` — so the set = this page's tokens (union across in-page re-renders). **Launcher:** `ensureI18nLauncher(on)` adds/removes a fixed `#vgc-sm-i18nlaunch` "🌐 i18n" pill (dev mode only). **Drawer** (`openI18nDrawer`, reuses `openDrawer`): pauses recording while building; snapshots `_i18nSeen`; lazy-fetches `/translations?lang=` once into `_i18nCat` (base/value/overridden) → per-token state badge translated/edited(over)/missing via `i18nState()`; **completeness scan** walks APP text nodes + placeholder/title/aria-label for catalogue keys present as literal English but NOT in the token set → flagged "not via t()" (catches unwrapped strings). Rows: click `<code>` to copy token; inline `<input>` per token. **Save** (`bindI18n`) batches edits → `POST /translations {lang, strings}` → applies live (`STR[k]=val`, `HAS_STR=true`) → `closeDrawer()` + `router()` re-render so the new wording shows immediately; reopen shows updated state. English (`boot.lang==='en'`): read-only list (source has nothing to translate); degrades gracefully if `/translations` GET is forbidden (no scan/flags, list still works). CSS: `.vgc-sm-i18nlaunch` (bottom-left, clears bottom nav on mobile, print-hidden) + `.vgc-sm-i18nrow*`. No devids (dev tool). Harness (bg + catalogue): launcher present, 21 tokens on /items, states exact (Low=translated, Archived/Search=edited, All categories=missing), scan caught an injected unwrapped "Reports", filter, edit Low→POST {lang:bg,strings:{Low:…}}→live re-render (old gone, new shown)→reopen edited; dev-off removes launcher; en read-only + note; 19/20 routes clean (item/1 = harness fixture gap); console clean. |
| **1.60.0** | **Template/logic split — orders domain. REFACTOR COMPLETE: all 7 domains split (shop, consignment, partners, purchases, admin, catalogue, orders).** `tpl/orders.tpl.js` (12 fns → `V.tpl.orders`): `listPage`, `noPartners`, `orderTiles(tt)` (initial render + every `refresh()`), **`lineRow(l, idx, stillOut)`** (normal vs display-piece branch; stillOut passed in), `salesCard(items)` (report rows prepared by logic — title/sub/paid), `paymentsCard({payments, balance, today})`, **`docPage(o)`** (o.doc null for new; termsValue resolved by logic; normal/display line groups prebuilt; salesHtml/paymentsHtml composed in), `searchResults`, **`saleRow(l, {avail, price, qty, amount})`** + **`saleDrawer`** (all availability/draft math in logic), **`printSheet(o)`** (rows arrive as {name, sku, qty, unitPrice, vat, lineTotal} — billable×netUnit math in logic) + `printBar`. `orders.js` logic-only (~560 ln): st/lines/payments/saleReports state, billable/stripVat/netUnit/netCost/stillOutOf/totals (mirrors `VGC_SM_Orders::strip_vat`), syncForm, refresh, sale-draft state machine (avail caps, auto-price vs manual-amount detection), print-data assembly, save/complete/payments/sale-report endpoints, print-overlay lifecycle. Harness (consignment fixture, hand-computed): tiles EXACT (net 40 / VAT 8 / gross 58 w/ customer-paid transport / paid 20 / balance 38 / margin 24 = 60% / out 7), line margin + still-out readouts, display row (no price inputs, separate on-loan section), live edits (qty_sold ripple, **incl-VAT toggle → net 33.33/gross 50/margin 20**, transport-payer flip, mode→full re-render hides sales card, disp-tick zeroes money), add/remove line (cost gross-up rule), **sale drawer** (avail 5, stepper auto-price 2→€20, cap at 5, manual-amount survives stepping, edit prefills qty 4/€40/actual 35/inv + add-back avail 9, save-then-POST/PUT, delete), payments add/delete, complete toast, save→redirect, **print sheet** (letterhead+VAT, billed-to/mode/transport/note, row 4×€10@20%=€48, display-pieces line, totals 58/20/38, footer+IBAN, Esc closes), new-order defaults (`?partner=`, partner terms 0), 22-route regression, phone stacked no-h-scroll, console clean. Dev-guide §5 marked COMPLETE; §10 gained the stranded-cross-file-call gotcha. |
| **1.59.0** | **Template/logic split — catalogue domain + BUGFIX (item-form Scan button).** `tpl/catalogue.tpl.js` (27 fns → `V.tpl.catalogue`, the largest tpl): list (`listPage`/`rowsDesk`/`rowsPhone`/`itemsEmpty`/`itemsLoadError`), quick-add (`qaDrawer`, **`qaCatSelect` exported** — the add-new-category flow replaces the select's outerHTML and rebinds), item page (**`itemPage({i, moves, desk, canWrite, canManage, canPublish, canUnpublish})`** composing hero/stock/store/actions/movements + `priceMarginStrip`; async shop-stock states `shopStockChecking`/`shopStockNotFound`/`shopStockLine`), panels (`removeBlocked`/`removeCard`/`movementCard`), item form (**`formPage`** incl. `pricingHtml` internal; exported `unitSelect`/`categorySelect` for outerHTML-rebind flows; `imgPlaceholder`/`imgUploading`/`imgTag`; `marginLine`/`marginEmpty` for the live margin readouts), recipe (**`compRow(c, idx, {opts, showAddUnit})`** — compatible-unit options computed by logic's dimension math and passed in, `extraRow`, `recipePage`, `recipeSearchResults`). `catalogue.js` logic-only (~630 ln): itemsState/ITEM_COLS, all binders, net↔gross↔pack conversions, margin math, unit-dimension conversion + recipe cost engine. **BUG FOUND & FIXED:** since the 1.52.0 module split, the item form's barcode **Scan** button called bare `scanOnce(` — which had moved to production.js — so every click threw a silent ReferenceError; now `V.screens.scanOnce(...)` (the Phase-2 splitter checked extracted-vs-kept refs but not extracted-vs-extracted — gotcha for future splits). Harness: list (rows/sort/scopes/low-chip/fab/calc-pill), item page exact (packs line, at-partners/held/store metas, both margin strips, buildable banner, 9 actions incl. publish/unpublish, shop-stock differs tag), movement form (pack mode ×12 hint, scrap-reason guard, save→reload), remove panel (archive-only w/ 5-movements warning, cancel), quick-add (price-wrap toggle, category add-new POST + select rebuild, save+close), item form (calc-cost block, manual toggle reveal, net 5→gross 6→pack 60, margins 75%/58.3% then 50% vs manual cost, gross 24→net 20, **Scan no-throw**), recipe (cost engine exact: 3.00/2.00/1.00/72.00; kg→g conversion; qty & yield recompute; mass-only unit opts; save PUT), 26-route regression, phone (cards, stacked item, no h-scroll), console clean. |
| **1.58.0** | **Template/logic split — admin domain (8 screens).** `tpl/admin.tpl.js` (21 fns → `V.tpl.admin`): `settingsPage({s, siteName, devMode})` (boot facts passed in) + `testing`/`testOk`/`testFail`, `categoriesPage`, `reportsPage({stock, prod, days, desk})` + `stockRowsDesk`/`stockRowsPhone`, `importPage({cols, canManage})` + `importNothing`/`importPreview`/`importing`/`importResult`/`importFailed`, `helpToc(shown, activeId)` (grouping in tpl; FILTERING in logic) + `helpArticle`/`helpPage`, `trRows(list)` (each row arrives with its display `val` — the edits-map lookup stays in logic) + `translationsPage`, `teamPage` (**TEAM_LEVELS/TEAM_HELP moved into the tpl** — presentation config), `auditPage({d, state})`. `admin.js` logic-only (~430 ln): guards, reportDays/helpCache+helpState/trState/auditState, edits map, CSV preview→payload mapping, all binding. Harness: settings (prefill, token-kept hint, site-name placeholder, test-connection 400 banner, print-save updates `boot.print` live), categories (rows + add POST), reports (KPIs 24/−2/€42, meta line, chip 30d→90d refetch + active state, stock table sorted value-desc w/ low-red), import (paste→preview 3 rows/1 bad→run→2 created·1 updated + error row), help (2 groups/3 links, active section, article body, search filters toc to 1, `?s=` deep link), translations (3 rows, edited pill, live edit + only-edited filter → 2 rows, save POST), team (locked admin tag, level-change POST + toast), audit (pager 1/2→2/2, filter apply refetch `search=`, clear resets), 21-route regression, console clean. |
| **1.57.0** | **Template/logic split — purchases domain.** `tpl/purchases.tpl.js` (7 fns → `V.tpl.purchases`): `listPage`, `costBasis(st)` (incl./excl. VAT qualifier), `purchaseTiles(tt)` (used by initial render AND `refreshTotals`), **`lineRow(l, idx, ctx)`** (`ctx = {vatOn, basis, sell}` — the computed gross sell price arrives as a number; netCost/sellNet/sellGross/billable stay in logic, mirroring `VGC_SM_Purchases::strip_vat`), `paymentsCard({payments, canWrite, today})`, `searchResults`, **`docPage(o)`** (`o.doc` null for new; `tilesHtml`/`linesHtml`/`marginPct` prebuilt by logic; `field`/`check` form idioms internal to the tpl). `purchases.js` logic-only (~350 ln): st/lines/payments state, all money math + totals, syncForm (`[data-s]`/`[data-f]` scrape), wire (shape-changing selects re-render; inputs → refreshTotals), addLine (unregistered vendor ⇒ vat_rate 0), quick-add via `V.screens.openItemQuickAdd`. Harness (hand-computed fixture: consignment, VAT-reg vendor, prices-incl-VAT, transport 10 we-pay, 2 lines, 1 payment): tiles matched expectations EXACTLY (net 60 / VAT 12 / gross 82 / paid 30 / balance 52 / retail 87 / profit 17 = 19.5% / held 5; L1 sell 18), live edits (transport→gross/balance/profit, qty_sold→cost/retail/held, markup→sell display, hint margin %), apply-markup-to-all, **vendor-VAT toggle re-render** (VAT column → n/a, incl-VAT checkbox disappears, basis flips excl.), mode→buy (held 0), remove/add line (search result carries cost_net + item VAT; new line under unregistered vendor gets rate 0), payments add (save-then-POST) + delete, complete + reopen toast, save→redirect, new-purchase query defaults (`?partner=&mode=`), **vendor pick adopts VAT status + terms**, quick-add drawer cross-file, list variants (overdue/consignment/final-price-no-VAT/completed/negative-profit red), 24-route regression, console clean. |
| **1.56.0** | **Template/logic split — partners domain.** `tpl/partners.tpl.js` (17 fns → `V.tpl.partners`): `listPage`(+`listRow`), form templates `formCard`/`locRow`/`conRow` (logic maps rows into `#pa-locs`/`#pa-cons` and rebinds), partner-page templates — `pageShell({p, actionsHtml, tabs, activeTab})`, **`actionsBar({id, canWrite, canManage})`** (permission flags passed in as data; the old always-true `isOut`/`isIn` ternaries collapsed), `summaryTiles`, `roleTags`, `consignTable`, `directoryCard`, `ordersCard`, `purchasesCard`, `outstandingCard`/`heldCard`/`notesCard`, `pricesCard`, **`recentCard(docs)`** (doc merging/sorting/slicing stays in logic — tpl renders the prepared list), `priceSearchResults` — and `statementPage(d, rangeText, partnerId)` (route id passed for the back link rather than trusting `d.partner.id`). `partners.js` logic-only (~300 ln): partnerRole (kept, currently unused), form read/render loops, tab state + renderTab/bindBody, savePrice, statement range/CSV. Harness (full-partner fixture: 2 locations w/ per-location contact, extra contact, 2 orders (overdue+completed), consignment purchase, note, price, outstanding+held): list rows (turnover 600 = orders+purchases values, owes/you-owe, no-trade variant), header+11 actions+6 tabs, overview tiles + recent docs merged newest-first, orders/purchases tabs (overdue/completed/consignment/still-held), consignment tab 3 cards, prices tab (edit binding, search→prompt→POST→reload), details directory, edit form (prefill, add/remove loc rows), new-partner save→`#/partner/9`, statement (all 10 tiles incl. overdue qualifier + margin %, profit note, settled-row dimming, they-owe vs you-owe columns, order/note links, apply-range refetch), row-click navigation, phone (list, 6 tabs, no h-scroll), 26-route regression, console clean. |
| **1.55.0** | **Template/logic split — consignment domain.** `tpl/consignment.tpl.js` (19 fns → `V.tpl.consignment`): `notesPage`, `editorNoPartners`, **`editorPage`** (form shell; takes `linesHtml` pre-built by logic + `totals`), **`lineRow(l, idx, grossUnit)`** (gross display value passed IN — the net↔gross math stays in logic, no formula duplication), `searchResults`, `issueProblems`, **`issuedPage(n, today)`** (`today` passed as param for the overdue check — templates never call `new Date()`), `outstandingPage`/`outstandingEmpty`/`outstandingRowsDesk`, **`consignRowsPhone`** (shared by outstanding+held — identical markup deduped), `heldPage`/`heldEmpty`/`heldRowsDesk`, recon flow (`reconChecking`/`reconClean`/`reconTable`/`reconBooking`/`reconError`). **`NOTE_TYPES`/`NOTE_GROUPS`/`typeHint` moved INTO the template file** (pure presentation config; logic reads the type from the DOM select, never from NOTE_TYPES). `consignment.js` logic-only (~440 ln): money math (netOf/vatOf/grossUnitOf/totals), syncForm, payload, all binding. Harness (method+status-aware fetch stub added — supports `[regex, body, method?, status?]`, used to simulate the 400 issue-validation): editor initial render exact (all fields, both line variants, totals), gross→net back-calc, VAT snap, qty totals, type-switch re-render keeps typed state + shows refund box, partner-switch resets location, search-add w/ item VAT, line remove, **issue → structured problems card** (400 w/ `data.problems`), issued view (status/sent/due+overdue tags, VAT, destination, totals, owes-note, paid/cancel/print btns), outstanding (KPIs, display tag, value-desc default sort, click-sort), held + full reconcile flow (check→table→book→toast+reload, cancel path), phone branches, 26-route regression, console clean. ⚠️ Harness gotcha: the Browser-pane native width can be <900px — set an explicit 1280px viewport before testing `isDesk()` branches. |
| **1.54.0** | **Template/logic split, Phase A (pilot) — shop domain.** New layer: `app/js/tpl/<domain>.tpl.js` → `V.tpl.<domain>` (namespace created in core: `V = { screens:{}, tpl:{} }`). **Template contract:** pure data→HTML-string functions; may call core atoms (esc/fmt/money/btn/icon/sortTh/emptyState…) + sibling templates; NO document/fetch/state — environment facts (e.g. `desk: isDesk()`) passed in as data. `tpl/shop.tpl.js` (17 fns): shop — `page` (status card + cat filter + `#shop-body`), `rowsDesk` (sortable table via sortTh, `[data-open]` rows), `rowsPhone`, `bodyEmpty`/`bodyNoMatch`, `notConnected(showSetup)`, `shopQty()` (— / not-on-shop / qty); pull — `pullShell`, `pullBody({rows,picks,desk})` (+`pullRowDesk/Phone`), `pullPager`, `pullNotConnected`/`pullNoProducts`/`pullErrorPage`/`pullLoadingRows`/`pullImporting`/`pullImportFailed`/`pullResult`. `shop.js` is now logic-only (fetch, SHOP_COLS/shopCategory/picks state, event binding), preamble grabs `var T = V.tpl.shop`. Load order: tpl files after core, before logic (`app_scripts()`). **Tooling:** codemap + components.py now walk `app/js/` recursively (tpl/ included). Dev-guide §5 documents the contract; extraction is NOT mechanical (closure vars → params), so verification = behaviour, not just render: harness checked shop status math/filter/sort/row variants, pull auto-tick rules (skip already/virtual), cross-page selection persistence, select/clear-page, import result incl. error rows, phone branches of both screens, 26-route regression, console clean. Next domains (each 1 release): consignment → partners → purchases → admin → catalogue → orders. |
| **1.53.0** | **JS split, Phases 3+4 — refactor complete; screens-legacy.js deleted.** New files: `partners.js` (~650 ln: partnerRole/viewPartners/partnerForm/viewPartner/viewStatement), `purchases.js` (~569), `orders.js` (~852), `consignment.js` (~743: NOTE_TYPES/GROUPS, viewNotes/viewNote/viewOutstanding/viewHeld), `admin.js` (~744: settings, categories, reports, import, help, translations, team, audit). The **CSV quartet** (`csvCell`/`downloadCsv`/`splitCsvLine`/`parseCsv`) was cross-domain (reports/import + statement + outstanding/held) → moved into **core.js** and exported. `app_scripts()` lists 9 files: core, catalogue, production, shop, partners, purchases, orders, consignment, admin. Splitter enforced line-coverage (no dropped lines), no core-export collisions, no cross-file stray refs. Harness: all 34 routes, quick-add cross-file, partner tabs + Prices tab, statement CSV button, console clean. **Phase 4 (tooling):** codemap + components.py walk `app/js/*` (done in Ph1), dev-guide §1/§2/§5/§9/§11 rewritten for the module layout (registry contract, per-file skeleton, add-a-screen recipe, collision warning, harness recipe now loads the file list + `VGCSM.start()`). Total JS: 6,525 lines / 9 files; largest is catalogue.js (~1,136). |
| **1.52.0** | **JS split, Phase 2 — catalogue.js / production.js / shop.js extracted from screens-legacy.js.** `catalogue.js` (~1,136 ln): itemsState + viewItems/loadItems/renderItems, openItemQuickAdd, viewItem/removePanel/movementForm, viewItemForm, viewRecipe. `production.js` (~300 ln): viewProduce, viewScan/startScanner/stopScanner/scanOnce (scanOnce now registered). `shop.js` (~424 ln): SHOP_COLS/shopCategory/viewShop + PULL_PER/viewPull. `screens-legacy.js` down to ~3,448 ln (settings/categories/reports/CSV helpers/import/partners/statement/purchases/orders/notes/consignment/help/translations/team/audit). ONE cross-file call patched: the purchase screen's New-item button → `V.screens.openItemQuickAdd(...)` (registry call at call-time; extracted files load before legacy but never rely on it). Splitter enforced: no name clashes with core exports, no extracted↔kept stray references. `Frontend::app_scripts()` now lists 5 files (order: core, catalogue, production, shop, legacy). Harness: all 34 routes error-free, purchase quick-add drawer works cross-file, 34 registered screens, console clean. |
| **1.51.0** | **JS split, Phase 1 — app.js → app/js/core.js + app/js/screens-legacy.js (registry architecture).** app.js (6,059 ln) deleted. **core.js** (~1,180 ln): all utilities/REST/queue/chrome + `viewDashboard`, plus shared helpers relocated from screen regions (`val`, `printLetterhead`, `statusTag`, `notesListHtml`), `parseHash`, the dev-mode block, and the **rewritten router** — dispatches via `V.screens.viewX(...)` (`S.itemsState.category` for the catalogue route). Core ends by building `window.VGCSM` (`V`): exports every core function + `boot`/`APP`, `V.register(obj)` merges into `V.screens`, and `V.start()` holds the old boot block (barcode-prefill wrapper now wraps `S.viewItemForm`, global search uses `S.itemsState`/`S.loadItems`, SW registration, `flushQueue()`, `router()`). **screens-legacy.js** (~5,190 ln): all screens verbatim inside its own IIFE — preamble destructures 51 helpers off `V` (`var esc = V.esc; …`), tail registers 27 view fns + `stopScanner`/`loadItems`/`itemsState`/`openItemQuickAdd`/`partnerForm`/`movementForm`. **PHP:** `Frontend::app_scripts()` returns the ordered file list; render_app loops `<script>` tags + `<script>window.VGCSM.start();</script>`; SW precache built from the same list (add a file in ONE place). **Tooling:** codemap + components.py walk `app/js/*.js`. Verified in a served harness: all 34 routes error-free, dev-mode badges (incl. after partner-tab swaps), purchase quick-add drawer w/ categories, console clean. Screen files must never declare a top-level name that collides with a core export (var-over-function hoisting would silently shadow it). |
| **1.50.0** | **Logout fix — service worker was swallowing the auth redirect.** The Sign out link is a plain `<a href=boot.logoutUrl>` (nonce'd `?vgc_sm_logout=1`); the server handler in `maybe_render()` is correct. The bug was client-side: the PWA service worker's `navigate` branch does `fetch(req)`, which follows the server's 302 and returns a `redirected:true` response — which the browser refuses to use for a navigation, so `respondWith` errors and the click does nothing. Fix in `output_service_worker()` (`class-frontend.php`): the fetch handler now parses the URL up front and, for any request whose query has `vgc_sm_logout` or `vgc_sm_login`, `return`s without `respondWith` so auth flows perform a normal, unmediated browser navigation to the server. SW re-installs automatically on the version bump (cache name is `vgc-sm-<VERSION>`); users must reload once after updating for the new worker to activate. Server-generated SW → not exercisable in the JS harness; verified by PHP lint + reasoning. |
| **1.49.0** | **Partner page → compact tabbed layout (design v2 `scPartner`).** `viewPartner` rewritten: fetch once, then a fixed **header** (`partner-header`) + **action bar** (`partner-actions`, incl. Edit) + **tab strip** (`partner-tabs`) over a single `#pa-body` that swaps client-side (`renderTab()` → `bodyHtml()` + `bindBody()` + `decorateDevMode()`), **no re-fetch on tab switch** (important — the partner fetch now computes turnover). Tabs: **Overview** (`partner-summary` tiles + `partner-recent` = latest orders/purchases/notes merged, newest first, 6), **Orders** (`partner-orders`), **Purchases** (`partner-purchases`), **Consignment** (`partner-outstanding`+`partner-held`+`partner-notes`), **Prices** (`partner-prices`, search-to-add + inline edit/del bound only on this tab), **Details** (`partner-directory`). Edit-partner stays in the fixed action bar → `partnerForm(p)` mounts into a fixed `#pa-form`. New CSS: `button.vgc-sm-subtab` reset + `.vgc-sm-subtabs--scroll` (nowrap/overflow-x). All prior partner devids preserved (relocated into tabs) + added `partner-tabs`, `partner-recent`. Verified in a harness: 6 tabs, correct per-tab content, **fetch count stayed 1** across all switches, prices search/edit + edit-partner form + row clicks work, dev badges reapply per tab, mobile no h-scroll, console clean. |
| **1.48.0** | **Partners list — turnover per row (financial always visible).** A settled partner (owed=0, we_owe=0) showed no money on its row; now every traded partner shows **total value traded** as the headline. `shape_partner` adds `orders_value` (`VGC_SM_Orders::turnover_for_partner`) + `purchases_value` (`VGC_SM_Purchases::spend_for_partner`) — each sums `totals()['gross']` over the partner's orders/purchases (all statuses, limit 500). Row `.vgc-sm-partrow__bal` now: `.vgc-sm-partrow__turnover` = `money(orders_value+purchases_value)` + a small uppercase "traded" label (shown when >0), then `owes €X`/`you owe €X` beneath when non-zero. `.is-owes` softened to `--vgc-ink-2`, amt to 12.5px (secondary to turnover). **Perf note:** turnover loads each of the partner's orders/purchases (via `get()`+`totals()`) — fine at workshop scale (few partners); if partner/doc counts grow, cache order/purchase gross or aggregate in SQL. Verified in a harness (settled customer → "€2,340.50 traded" only; owing customer → turnover + owes; supplier → turnover + you-owe; no-trade → nothing). |
| **1.47.0** | **Partners list — aligned to design v2 `scPartners`.** Header now shows the partner count (left) beside New-partner (right). Row avatar bumped to **42px** (`.vgc-sm-partrow .vgc-sm-ava { width/height:42px; font-size:16px }`; base `.vgc-sm-ava` stays 34px for team/audit). Balance switched to the design's inline format: `owes €X` (ink, `.is-owes` → `--vgc-ink`) / `you owe €X` (amber `.is-youowe` → `--vgc-brand-dark` #8A4417), dropping the stacked `<small>` labels; both lines show for a dual partner. Row `padding:13px 4px; min-height:66px` per design. The 1.46.0 trade counts (`N orders · M purchases · contact`) remain as the meta sub-line (an addition to the design, per the user's earlier request). Verified in a harness. |
| **1.46.0** | **Partners list — spacing + trade stats.** `viewPartners` row rebuilt: `avatarInitial(name)` + `.vgc-sm-partrow__meta` (flex column: name, role pills via `badge('Customer'/'Supplier'/'No trade yet')`, and a counts sub-line `N orders · M purchases` + contact) + `.vgc-sm-partrow__bal` (right column: `owes you` green / `you owe` red, each with a `<small>` label, both shown when a partner is on both sides). New CSS `.vgc-sm-partrow*` gives uniform, roomy rows (fixes the uneven spacing from the old `<br>`-in-`.vgc-sm-qty` layout). **Server:** `shape_partner` now returns `order_count`/`purchase_count` from new `VGC_SM_Orders::count_for_partner()` / `VGC_SM_Purchases::count_for_partner()` (single `COUNT(*)` each — cheap; the list already does the heavier `balance_owed`). Verified in a harness (customer-only/supplier-only/both/no-trade partners, correct counts incl. singular 'purchase', dual balances, uniform 87px rows, no h-scroll, console clean). |
| **1.45.0** | **Dedicated order print template (design v2 orderPrint).** Replaced print-by-reshaping-the-live-screen with a purpose-built document. `openOrderPrint()` (inside `viewOrder`, hoisted; the `#o-print` button calls it) builds an invoice-style `.vgc-sm-psheet` and shows it in a full-screen `.vgc-sm-printov` overlay (Close + Print/Save-PDF, both `.vgc-sm-noprint`). Sheet = letterhead (`boot.print.name/address/vat` + `◐` mark) · ORDER/number/dates/status · Billed-to (partner, first location, terms, mode) / Payment (terms, transport, note) · line table `#/Item/Qty/Unit price/VAT/Line total` using **billable qty × netUnit** (matches the order's own `totals()`) · display-pieces-on-loan note · Net/VAT/Total/Paid/Balance (green paid, red balance, from `tt`) · footer (`boot.print.footer` + IBAN). Fixed light palette (theme-independent). **Print isolation:** body gets `.vgc-sm-has-printov` while open; a dedicated `@media print` block hides `.vgc-sm-shell`/nav/toast/printbar and prints only the sheet full-width. **Server:** `VGC_SM_Sync::settings()` gained `biz_name/biz_address/biz_vat/biz_iban/print_footer`; `save_settings` guards `auto_push` (keep-current when absent); `trait-shop` `save_settings` only forwards **present** params (so a partial save can't blank the connection) and `get_settings` returns the biz fields; `render_app` boot exposes `print{name(fallback siteName)/address/vat/iban/footer}`. **SPA:** new Settings **Printed documents** card (`settings-print`, own Save → POST /settings biz-only → updates `boot.print` live). Verified in a harness (sheet numbers Net120/VAT24/Total156-incl-transport/Paid100/Balance56, billable qty 10 & 4-after-return, display note, letterhead/footer/IBAN; print-emulation hides shell+printbar, sheet full-width no-shadow; Close/Escape dismiss; console clean). Note: the old generic `@media print` (1.34.0, tiles etc.) still governs statement/partner printing. |
| **1.44.0** | **Fix: over-tall "Markup on all lines" input (purchase-lines).** `#p-defmarkup` is a `.vgc-sm-input` inside `.vgc-sm-toolbar` (`.vgc-sm-toolbar .vgc-sm-input { flex: 1 1 200px }`) **and** wrapped in a `.vgc-sm-of` (a *column* flex) — so the `200px` flex-basis applied to the item's main axis = **height**, rendering it ~200px tall. Pinned the input inline: `width:90px;height:34px;flex:none` (the `flex:none` stops the vertical stretch; resolves to the standard 44px control height). **Gotcha for later:** don't drop a bare `.vgc-sm-input` into a `.vgc-sm-toolbar` when it's also inside a `.vgc-sm-of` column — the toolbar's `flex:1 1 200px` becomes a height. Verified in a harness (height matches `#p-mode` = 44px, width 90px, console clean). |
| **1.43.0** | **Fix: categories in the purchase quick-add drawer.** `openItemQuickAdd` was called with `[]` cats from the purchase, so its category `<select>` was empty and had no add-new. Now it **loads categories itself** (`get('/categories')` when `cats` empty) and its select is built by a local `catSelectHtml(list,current)` that includes a `＋ Add new category…` (`__add__`) option, wired by `bindCat(list)` → prompt → `POST /categories` → rebuild select with `created_id` selected → `refreshCategories()` (global) → toast — mirroring the item form's `categorySelectHtml`/`bindCategorySelect`. Restructured into an inner `build(catList)` so the fetch can precede drawer open. Save guards `category_id: (val('qa-cat')==='__add__' ? '' : ...)`. Verified in a harness: drawer lists existing cats + add-new; add-new POSTs, rebuilds selected, toasts; item save carries the new `category_id`; console clean. |
| **1.42.0** | **Add-item flow: full page in catalogue, quick-add drawer on purchase (user request).** Reverted the catalogue `items-add` button + `.vgc-sm-fab` back to `linkBtn(...newHref)` plain links to `#/item/new` (the full item form, as pre-1.35.0); removed the `openItemQuickAdd` click-wiring from `viewItems`. The quick-add drawer now serves the **Purchase** screen: `wireQuickItem()` (the `#p-newitem` handler) opens `openItemQuickAdd([], 'products', {title, subtitle, onSaved})` and its `onSaved(it)` calls the purchase's `addLine(...)` — replacing the old inline `quickItemHtml()`/`#p-new-wrap` form (both removed). `openItemQuickAdd` generalised: `opts.onSaved(item)` (hands back the created item instead of `loadItems()`), `opts.title`/`opts.subtitle`, and the **Full-form link is hidden** when `onSaved` is set (navigating away would abandon the purchase). SKU is now **optional** (blank → server auto-generates), matching the old purchase quick-add; validation requires name only. **Bug fixed during the change:** a leftover `sku` variable reference (after relaxing validation) threw in the save handler and silently blocked the POST — now `val('qa-sku')`. Verified in a harness: catalogue Add/FAB are anchors to `#/item/new` (no drawer); purchase New-item opens the drawer (no full-form link, SKU optional), save POSTs the item and adds the line, drawer closes, console clean. No component-ID (`data-devid`) changes. |
| **1.41.0** | **Dev mode Phase 5 — admin screens; roll-out COMPLETE (tagging only).** Settings: `settings-connection`, `settings-devmode`. Team: `team-help`, `team-list`. Translations: `translations-panel`. Activity log: `audit-filter`, `audit-list`. Import: `import-form`. **Login is intentionally NOT tagged** — `render_login()` is PHP-rendered *before* auth and never loads `app.js`, so the JS badge machinery (and the per-user `boot.devMode`) can't run there; noted in `components.md`. **88 component IDs across every in-app screen, all documented.** Verified in a harness: settings/team/audit/translations/import each render their badges, no errors, console clean. **Dev-mode component-ID system is complete** (Phases 1–5: 1.36.0 mechanism → 1.41.0 full coverage). To act on a user's `page-component` id, grep `data-devid="<id>"` (or, for itemform/home-low dynamic ones, the helper call) in app.js. |
| **1.40.0** | **Dev mode Phase 4 — home / notes / consignment / shop tags (tagging only).** Home: `home-quick`, `home-low-materials`, `home-low-products` (dynamic — `lowCard(title,rows,empty,kind,devid)` gained a devid param), `home-shop`. Notes list: `notes-toolbar`, `notes-list`. Note editor: `note-details`, `note-lines`, `note-total`, `note-actions`. Issued note (`renderIssued`): `noteview-header`, `noteview-lines`, `noteview-total`. Consignment: `outstanding-list`, `held-list`. Pull: `pull-options`, `pull-products`. Shop: `shop-status`, `shop-items` (tagged the *card*, not the re-rendered `#shop-body` inside it). components.py dynamic-devid capture broadened to `'((?:itemform|home-low)-[a-z]+)'`. 78 IDs / 21 prefix-groups, all documented. Verified in a harness: home shows all 4 (incl. the two dynamic low cards), notes list clean, no errors, console clean. **Next: Phase 5** (settings, team, translations, audit, import, login) — the final batch. |
| **1.39.0** | **Dev mode Phase 3 — catalogue & production tags (tagging only).** Catalogue: `catalogue-toolbar`, `catalogue-filters`, `catalogue-list` (**wrapped** — `renderItems()` replaces `#items-list` innerHTML, so the devid sits on an outer `<div>`). Item form: `itemform-identity/classification/sourcing/packaging/pricing/actions` — the `group(title, inner, devid)` helper gained a 3rd param that emits `data-devid` (so these are **dynamic**, not literal). Recipe: `recipe-header/components/othercosts/cost/actions`. Produce: `produce-picker` (**wrapped** — `pick()` replaces `#p-pick` innerHTML) + `produce-consumes/plan/cost/scrap/run` which render into `#p-plan` **after** "Check", so `checkPlan()` now calls `decorateDevMode()` after setting `box.innerHTML` to re-inject badges. Scan: `scan-camera` (**wrapped**), `scan-entry`. **components.py updated:** also captures the `group()` dynamic devids via `'(itemform-[a-z]+)'` and filters to strict kebab (drops the helper's own `data-devid="' + devid + '"` construction). 61 IDs / 13 pages, all documented. Verified in a harness: catalogue-list badge survives `renderItems`, item-form shows all 6 group badges, produce/scan clean, console clean. **Rule reminder:** wrap (don't directly tag) any element whose `innerHTML` is replaced after render; call `decorateDevMode()` after any post-render innerHTML swap that should show badges. **Next: Phase 4** (notes list/editor, consignment out/held, pull, home, shop). |
| **1.38.0** | **Dev mode Phase 2 — trade & partner screen tags (tagging only).** Added `data-devid` to: Orders list (`orders-toolbar`, `orders-list`), Purchases list (`purchases-toolbar`, `purchases-list`), Partners list (`partners-toolbar`, `partners-list`), **Order** (`order-header` [docHeader wrapped], `order-summary`, `order-details`, `order-lines`, `order-onloan`, `order-sales` [in `salesCardHtml`], `order-payments`, `order-actions`), **Purchase** (`purchase-header`, `purchase-summary`, `purchase-details`, `purchase-lines`, `purchase-payments`, `purchase-actions`), **Statement** (`statement-filter`, `statement-tiles` [wraps `statTiles`], `statement-timeline`, `statement-actions`), **Partner** (`partner-header`, `partner-summary`, `partner-actions`, `partner-directory`, `partner-outstanding`, `partner-held`, `partner-orders`, `partner-purchases`, `partner-notes`, `partner-prices`) — **39 IDs / 8 pages**, all in `components.md`. **Gotcha fixed & noted for later phases:** `decorateDevMode()` appends the badge as a *child* of the tagged element, so tagging a container whose `innerHTML` is later replaced in place (here `#o-summary`/`#p-summary` via `refresh()`/`refreshTotals()`) **wipes the badge** → moved those two devids onto a non-re-rendered wrapper `<div data-devid=…><div id=…>tiles</div></div>`. **Rule for Phase 3+: never put `data-devid` directly on an element that gets `el.innerHTML = …` after render (e.g. catalogue `#items-list` via `renderItems`) — wrap it instead.** Verified in a harness: Order 6 badges (incl. order-summary after the fix), Partner 8, no errors, console clean. **Next: Phase 3** (catalogue, item form, recipe, produce, scan). |
| **1.37.0** | **Dev mode — clickable badge + comment popover.** Replaced the pure-CSS `::before` label (top-left, inert) with a **real clickable `.vgc-sm-devbadge`** at each panel's **top-right**, injected by `decorateDevMode()` which now runs at the end of `screen()` (every render) and inside `applyDevMode()`. Clicking a badge opens `openDevPop(badge,id)` — a fixed-position `.vgc-sm-devpop` (id title + `<textarea>` + Copy/Close via `btn()`), positioned under the badge and clamped to the viewport. **Copy** → `copyText('In `+'`'+`id`+'`'+`: '+note)` (empty note → just `` `id` ``) with a "paste it to the dev" toast; closes after. Dismiss on outside `mousedown` (capture) or Escape. Removed the old Alt+click id-copy (superseded). New CSS: `.vgc-sm-devbadge` (abs top:0/right:0, translateY(-100%), cursor pointer), `.vgc-sm-devpop(__id/__note/__foot)`; print-hides both. `data-devid` tags unchanged (still Item detail only). Verified in a harness: 5 right-side badges, click→popover, comment copy = `` In `item-header`: make the image larger ``, empty = `` `item-stock` ``, outside-click dismiss, badges removed when off (panels stay tagged), console clean. Updated `components.md` (interaction section). **Next: Phase 2** tags the trade/partner screens. |
| **1.36.0** | **Dev mode Phase 1 — component-ID overlay (the mechanism).** A per-user aid: turn on *Developer mode* (Settings) and every tagged panel shows its **component ID** badge + dashed outline, so change requests can name the exact block. **Server:** `set_dev_mode()` in `trait-admin.php` + route `POST /me/dev-mode` (perm `$auth`, body `{on}`) → `update_user_meta(uid,'vgc_sm_dev_mode')`; `render_app()` boot gains `devMode => (bool) get_user_meta(...)`. **App (app.js):** `applyDevMode(on)` toggles `.vgc-sm-devmode` on `<body>`; `setDevMode(on)` also POSTs to persist; `applyDevMode(boot.devMode)` at init; a **capture-phase** document click handler copies `data-devid` on **Alt+click** (clipboard + bare-id toast fallback). Settings gains a *Developer mode* card (`#s-devmode`). **CSS:** `.vgc-sm-devmode [data-devid]` outline + `::before{content:attr(data-devid)}` badge (brand, mono, positioned above; nested tag → badge on the right); hidden in `@media print`. **Convention:** `page-component` (dash), lowercase, panels only. **Tags are always in the HTML (inert when off)** → toggling is instant, no re-render. Proof screen: **Item detail** — `item-header`, `item-stock`, `item-store`, `item-actions`, `item-movements`. **New repo docs:** `vgc-stock-manager-components.md` (the component map — page→component→id→description, with a Coverage checklist) and `vgc-stock-manager-components.py` (audit: lists ids grouped by page, flags any missing from the MD). Verified in a harness: 5 badges (correct ids/brand/dashed outline), class-toggle shows/hides instantly with attrs retained, Settings toggle POSTs `{on:0|1}` + flips class, Alt+click copies, badges gone when off, console clean. **Next: Phase 2** tags the trade/partner screens (Orders/Purchases lists, Order, Purchase, Statement, Partner, Partners) and fills in `components.md`. |
| **1.35.0** | **Redesign Phase 5 — item quick-add drawer (additive).** New `openItemQuickAdd(cats, scope)` (app.js, before item-detail): a slide-in via `openDrawer()` for fast item creation from the essentials — Name, SKU, `for_sale` (Product/Material, presets from the list `scope`), `kind` (raw/manufactured), Unit (static pcs/g/kg/ml/l), Category (from the catalogue's already-fetched `cats`), Unit cost, Retail price (hidden via `#qa-price-wrap` when Material), VAT%. Save → `write('/items','POST', body)` with the same field names as the full form (`is_sellable` mirrors `for_sale`, `reorder_level:0`, `store_stocked:0`), then `closeDrawer()` + `loadItems()`. name+SKU required. `viewItems`: the desktop **Add item** `linkBtn`→`btn(id="items-add")` opens the drawer; the mobile `.vgc-sm-fab` keeps `href=newHref` (no-JS fallback) but its click is intercepted (`preventDefault`) to open the drawer. **The rich full-page item form is untouched** and linked from the drawer footer (`Full form…` → `#/item/new`) — this is the user-approved *lightweight second create path*, NOT a replacement (they chose \"add a lightweight quick-add drawer\"). Verified in a harness: drawer opens with 9 fields, default Product on `/items`, retail hidden for Material, empty name/SKU blocked (no POST + warn toast), successful save POSTs the exact body (kind/for_sale/is_sellable/cost_net/price_net/vat_rate/category_id…), drawer closes + list reloads, full-form link present, console clean, no h-scroll. **Redesign complete** (Phases 0–5). |
| **1.34.0** | **Redesign Phase 5 — print polish for design-v2 primitives (print-only CSS).** The `@media print` block (app.css ~line 1047) predated Phases 1–4, so it flattened the old `.vgc-sm-statrow`/`.vgc-sm-kpis` but **not** the new primitives — a printed order/statement/partner showed the dark **hero tile as a solid black box**, plus avatars/icons as noise. Added print rules (inside the existing `@media print`, single-tab indent): `.vgc-sm-tiles` (force `repeat(4,1fr)`, tight gap, break-inside avoid), `.vgc-sm-tile` (bordered white), `.vgc-sm-tile--hero` + children (override the dark-inverted bg/text to white/black; value 14pt), `.vgc-sm-tile--good/--bad .vgc-sm-tile__v` (darkened ink greens/reds `#1a6b3a`/`#a01810`), `.vgc-sm-dochead`/`.vgc-sm-parthead` (keep words, `__ava`/`.vgc-sm-ava`/`.vgc-sm-noteicon` → `display:none`), `.vgc-sm-onloan`/`.vgc-sm-notebanner` (flatten tints to bordered white), `.vgc-sm-drawerov` → `display:none` (never print an open overlay). **Print-only — screen appearance untouched.** No JS/DB change. Verified in a browser harness by flipping the `@media print` `CSSMediaRule.media.mediaText` to `'all'` (Chrome print emulation isn't exposed via the MCP): hero dark `rgb(16,14,12)`→white/black, tiles 4-col, good=`#1a6b3a`/bad=`#a01810`, `.vgc-sm-back` hidden, `.vgc-sm-printhead` shown; and critically **before** the flip the hero was still dark (proves the rules don't leak to screen); console clean. **Design decision:** the design's other Phase-5 thread — converting the rich full-page item form (`viewItem`/item form: recipe/BOM, images, dual pricing, store distribution, barcode) into the simplified `itemDrawer` — was **NOT** done: it's a large lateral rewrite that would regress a working screen (same principle as 3c line-grid / 4e tables). The slide-in drawer pattern already ships where it's genuinely additive (Phase 3b Record-a-sale). Flagged to the user as optional. |
| **1.33.0** | **Redesign Phase 4e (SPA) — admin-screen avatars (visual only).** New helper `avatarInitial(name)` (app.js, after `statusBadge`) → `<span class="vgc-sm-ava">X</span>` (uppercased first letter). Added to `viewTeam()` User cell (avatar + name/username in a flex) and `viewAudit()` Who cell (avatar + name). New CSS `.vgc-sm-ava` (34px round, `--vgc-sunken` bg) — distinct from the 52px `.vgc-sm-parthead__ava`. `viewTranslations()` and `viewSettings()` were already clean forms/tables → left untouched (a table→list rewrite would regress, per the 4c principle). Access-level selects/admin-lock/audit filters/pagination unchanged. Verified in a browser harness: Team (avatars A/B/V, Administrator row locked, 2 editable `.tm-level` selects) + Audit (avatar per row), no h-scroll, console clean. **Phase 4 SPA screens complete (4a statement, 4b partner, 4c catalogue, 4d notes, 4e admin).** The **PHP sign-in screen** (`VGC_SM_Frontend::render_login()`) was then reviewed as its own step and found to **already implement design v2 `scLogin` exactly** — `.vgc-sm-auth__card` (max-width 400px, radius 16px, shadow `0 20px 50px -20px rgba(40,25,10,.28)`), `.vgc-sm-auth__mark` (56px, radius 15px, brand ◐), 50px fields, 52px brand button, host footer — so **no code change was warranted** (verified via a static replica of the render_login markup against the live app.css: every metric matched in light mode, and dark mode adapts all tokens cleanly — dark card/inputs, light text, brand-adjusted mark). **Phase 4 is complete.** No release for the login (nothing changed). |
| **1.32.0** | **Redesign Phase 4d — stock notes list + editor banner (visual only).** `notesListHtml()` (app.js): each row now leads with a `.vgc-sm-noteicon` tile (notes icon), and the title is `<span class="vgc-sm-mono">number</span>` + `badge(type_label, 'note')` (brand-tint pill) instead of "number · type_label" text; status tag + `money(total_gross)` + chev unchanged. `viewNote()` editor: the plain `<p class="vgc-sm-muted">typeHint</p>` became a `.vgc-sm-notebanner` (icon + `__t` type name from `NOTE_TYPES[type].label` + `__d` = `typeHint(type)`), re-rendered on type change like the rest of the form. New CSS (app.css "Phase 4d"): `.vgc-sm-noteicon`, `.vgc-sm-tag--note` (brand-tint), `.vgc-sm-notebanner(__t/__d)`. No change to the note form fields, lines, totals, issue/cancel, or `renderIssued`. Verified in a browser harness: list of 3 (Release/Sale report/Take-on-consignment incl. a null-number Draft) — icon tile, brand-tint pill, status, amount; editor banner renders and switches Release→Sale report live; no h-scroll; console clean. Next: 4e admin & login (team, translations, audit, login screen). |
| **1.31.0** | **Redesign Phase 4c — catalogue role badges (visual only).** `renderItems()` (app.js): each item now shows `roleBadges(i.for_sale, i.kind)` (Product/Material + Made/Bought pills) under its name. **Desktop** table: the standalone **Kind** column is gone — dropped `th('kind')` from the thead and the `<td>tagHtml(i.kind)</td>` cell; the item cell is now thumb + `<div>`name + badges`</div>` (6 headers = 6 cells). **Mobile** list: badges added after the sku/category `.vgc-sm-lsub` (gave the lsub `margin-bottom:5px`). `ITEM_COLS.kind` left defined but unreferenced (harmless); `tagHtml()` **kept** — still used by item detail (~line 898) and one other screen (~2293). Sorting/search/filters/`data-open` navigation unchanged. **Harness note:** `isDesk()`/table vs list is width-gated (~≥900px) — `resize_window` "reset to native" gave 765px and rendered the mobile list; had to set width:1280 explicitly to exercise the table. Verified desktop + mobile: correct badge pairs (Product·Made / Material·Bought), no Kind column, aligned headers/cells, SKU-sort re-render keeps badges, no h-scroll, console clean. Next: 4d stock notes (list type chips + tone-coloured qty; note editor description banner). |
| **1.30.0** | **Redesign Phase 4b — partner detail header + summary tiles (visual only).** `viewPartner()` (app.js): the old `<h1>` + VAT-meta line → a `.vgc-sm-parthead` block (round initial avatar `__ava`, `__name`, then `.vgc-sm-badges` with `badge('Customer','customer')`/`badge('Supplier','supplier')` from `p.is_customer`/`p.is_supplier` + a `VAT registered` pill, and the VAT number as a mono sub-line). The four `.vgc-sm-kpi` cards → `statTiles([...])` in a `.vgc-sm-summary` wrapper: Out on consignment (`fmt(outQty) pcs`), Owes you (tone `bad` if `p.overdue` else `null`/`good`), Held from them, You owe (tone `bad`>0 else `good`). Retired the local `kpis[]` array. **Kept** `.vgc-sm-kpi(s)` CSS — still used by other screens (home/reports, 18 refs). New CSS: `.vgc-sm-parthead(__ava/__meta/__name)` (+ name shrinks to 19px ≤560px). Everything else on the page (action bar, `directoryCard()`, consignment tables, orders/purchases tables, notes, price list, edit form) is byte-for-byte unchanged. **Harness gotcha:** `viewPartner` reads `p.notes.length` and `p.prices.length` — a mock without them throws "reading 'length'" into `errorView` (looks like your change broke it; it didn't). Verified desktop + mobile: avatar "C", name, [Customer, Supplier, VAT registered] pills, VAT number, 4 tiles (settled `You owe` green), no `.vgc-sm-kpis`, 11 actions + 2 consignment tables present, no h-scroll, console clean. Next: 4c catalogue (role badges + item list/cards). |
| **1.29.0** | **Redesign Phase 4a — partner statement KPI row → unified statTiles (visual only).** `viewStatement()` (app.js): the KPI block that used `statRow([stat(...)])` (pre-Phase-0 `.vgc-sm-statrow`/`.vgc-sm-stat`) now calls `statTiles([{l,v,q,tone}])`. Tiles: Turnover (hero, q "excl. VAT"), Billed (q "incl. VAT"), VAT (if any), Paid (good), **They owe you** (tone `bad` + q "overdue €X" when `tot.overdue` — the standalone Overdue tile is gone, folded in per the design), You owe them (if any), Profit (good/bad + q "margin N%"), Items sold, Documents, Average value. Retired the now-unused `stat()`/`statRow()`/`qual()` helpers (statement was their only caller). **Kept** the `.vgc-sm-statrow`/`.vgc-sm-stat` CSS + inline markup — still used by `viewItem`'s store/workshop 2-tile split (line ~966). No data/table/filter/print/CSV change. Verified in a browser harness (partner statement, one overdue + one part-paid + one settled line): 9 tiles with correct labels/values/tones, dark hero (bg #221F1A), computed red `They owe you` / green `Paid`, no `.vgc-sm-statrow` present, timeline table 3 rows, no h-scroll, console clean. Next: 4b partner detail (avatar + role badges header, action bar, subtabs, summary tiles). |
| **1.28.0** | **Redesign Phase 3c (light) — order/purchase line-grid polish (visual only).** User chose "light polish" over a full design-match after I flagged that the design's fixed-width horizontal-scroll grid would *regress* the current container-query responsive grid (which keeps all columns visible on desktop and self-labels each field when it wraps). So the responsive grid stays; only the look changes. app.css: `.vgc-sm-of__ro` and `.vgc-sm-oline/.vgc-sm-pline .vgc-sm-input[type="number"]` now use `font-family: var(--vgc-mono)` + right-align + tabular-nums (numbers read as aligned figures); new `.vgc-sm-of__m.is-good`/`.is-bad` colour the order Margin (green `--vgc-ok` / red `--vgc-danger`); the `@media (max-width:1099px)` blocks give each `.vgc-sm-oline`/`.vgc-sm-pline` a full border + radius + margin (per-line **cards** on phone), with `:first-child` border-top re-asserted (the base rule zeroes it). app.js: order `refresh()` toggles `is-good`/`is-bad` on the margin cell by sign. No markup/behaviour/DB change (still DB 0.21.0). Verified in a browser harness (order with a +margin and a −margin line): desktop shows green €7.00 / red −€6.00 margins, mono right-aligned inputs + read-only cells, lines stay flat (no card) with no h-scroll; mobile shows each line as a 10px-radius bordered card (incl. first), labels shown, no h-scroll; console clean. **Cache note:** the local http.server sends no-cache headers of its own, so the browser served stale app.css/js from the prior phase until asset URLs were given a `?v=` query — always cache-bust the harness after CSS/JS edits. |
| **1.27.0** | **Redesign Phase 3b — Record-a-sale UI on the Order screen.** Wires the 1.26.0 sale-report backend into `viewOrder()` (app.js). Local state gains `saleReports` from `o.sale_reports`. New helpers (hoisted in `viewOrder` scope): `isConsignment()` (=`payment_mode==='on_sale'`), `orderLineByItem`, `saleUnitPrice` (=line `unit_price` as typed), `saleAvail(itemId, report)` = `max(0, sent−returned−qty_sold)` **plus** the report's own line qty when editing (because `qty_sold` already aggregates every saved report), `saleLineSummary`, `salesCardHtml()`, and `openSaleDrawer(report)`. `salesCardHtml()` renders only when `o && isConsignment()` — a "Sales from sent stock" card (`.vgc-sm-salelogs`) with a Record-sale button and one tappable log row per report (edit) + a trash button (delete), inserted before the Payments card. `openSaleDrawer` builds a slide-in via `openDrawer()`: candidate rows (non-display, sent>0, avail>0 or already in the edited report) each with a `.vgc-sm-stepper` (capped at `saleAvail`) + a "Vendor pays" number input (auto = qty×unit_price unless the user typed a manual amount — tracked by comparing `round2(amount)` to the priced value), a Settlement block (actual paid + invoice), and a live footer (count + total, `#sale-due`/`#sale-total`/`#sale-count` patched in place, no re-render so focus survives). Save calls the order `save()` **first** (persist edits) then POST `/orders/{id}/sale-reports` (or PUT `/{rid}`), then swaps `o/lines/payments/saleReports` from the response and re-renders; delete confirms then DELETEs. New CSS (app.css, "Phase 3b" block): `.vgc-sm-drawer__title`, `.vgc-sm-salelog(s)`, `.vgc-sm-salerow(__name/__ctl)`, `.vgc-sm-saleamt`, `.vgc-sm-saleset(__grid)`, `.vgc-sm-salefoot(__tot)`; reuses the Phase 0 `.vgc-sm-stepper` + drawer head/body/foot. Verified in a browser harness (consignment order, one seeded report): card renders + gated off on pay-in-full; drawer lists the right candidates (display piece excluded, already-sold capped); steppers auto-price, cap at avail, manual override persists across a step; footer totals live; **add** saves order then POSTs the exact body (qty/amount/paid_actual/invoice/sold_on); **edit** prefills, avail adds own qty back, PUTs with rid; **delete** confirms + DELETEs; console clean. Line-grid cell restyle (`cellIn`/`cellRO`) + phone stacked line cards for Orders & Purchases still deferred (Phase 3c). |
| **1.26.0** | **Redesign Phase 3a — consignment sale-report backend (dormant; no UI).** Records the discrete "the vendor sold N of these" events on a consignment order. New tables `vgc_sm_order_sale_reports` (order_id, sold_on, invoice, paid_actual, total, payment_id) + `vgc_sm_order_sale_lines` (report_id, item_id, qty, amount); DB_VERSION **0.21.0** (added to `create_tables()` — no `--` comments; table helpers `vgc_sm_order_sale_reports_table()`/`vgc_sm_order_sale_lines_table()`). Model in `class-orders.php`: `sale_reports()`, `add_sale_report()`, `update_sale_report()`, `delete_sale_report()` (+ private `clean_sale_lines`/`apply_sold_deltas`/`sync_report_payment`). Design (delta-based, composes with manual edits): saving a report bumps each order line's `qty_sold` by the **delta** vs the report's prior lines, then calls `reconcile()`; `paid_actual` (defaults to the report total) is mirrored into an **order payment**, its id stored on the report and re-synced/removed on edit/delete. `avail` for a line = sent − returned − qty_sold (+ this report's own qty when editing). REST (in `trait-orders.php` + routes in `class-rest-api.php`): POST `/orders/{id}/sale-reports`, PUT/DELETE `/orders/{id}/sale-reports/{rid}`; `shape_order` now emits `sale_reports[]`. Verified with a **stubbed-`$wpdb` delta test** (24 assertions: add/update/delete qty_sold deltas, total, payment create/update/delete + linkback, reconcile ran, empty-lines rejected). No screen wired yet — the Record-a-sale drawer + per-order sales log land in **Phase 3b**. |
| **1.25.0** | **Redesign Phase 2 — Purchase document re-skin (visual only).** Mirror of Phase 1 on `viewPurchase()`: h1+completed tag → `docHeader(number, vendor, status, mode Bought/Consignment, href)`; the old `.vgc-sm-statrow` "Money" card (lower down) → a `statTiles` summary in `#p-summary` at the top (Cost·exVAT / VAT / We-owe-them-hero / Paid / Balance good-bad / Retail·exVAT / Potential-profit pct+profit / Still-held), re-rendered by `refreshTotals()`. New local `purchaseTiles(tt)`. The profit-assumption note moved under the tiles. All ids/`data-s`/`data-f` hooks and behaviour preserved. Verified in a browser harness: renders (hero We-owe €72.00 correct), editing qty_received re-renders summary (€132.00), Save body unchanged (line received=20), old statrow gone, console clean. Line grid (`.vgc-sm-pline`) cell restyle still deferred with Orders’ to after Phase 3. |
| 1.24.0 | **Redesign Phase 1 — Order document re-skin (visual only).** `viewOrder()` render rebuilt on the Phase 0 primitives, preserving every id/`data-*` hook and all behaviour (totals/save/reconcile/payments untouched). Changes: h1+status/overdue tags → `docHeader(number, partner, status, mode, href)`; the old `.vgc-sm-otot` Balance card → a `statTiles` summary in `#o-summary` at the top (Net/VAT/Total-hero/Paid/Balance/Margin+profit-qualifier/Still-out), re-rendered wholesale by `refresh()` so tone follows the numbers; display-pieces wrapped in a blue on-loan card (`.vgc-sm-onloan`, info-bg) — visual only, still inline-editable (mark-returned interaction deferred). New local `orderTiles(tt)`. Verified in a browser harness with a real order: renders, hero Total €57.36 correct, editing qty_sold re-renders the summary (€80.64) and per-line still-out/margin, Save PUT body unchanged (line sold=6, VAT flag, 3 lines), blue card holds the display line, payments/actions intact, console clean, no h-scroll. Deferred to Phase 1b: line-grid cell restyle (`cellIn`/`cellRO`) + phone stacked line cards. |
| 1.23.0 | **Redesign Phase 0 — foundations (additive, dormant).** Imported the Claude Design v2 artifact (`vgc-stock-manager-design-v2.html`, a JS prototype) and translated its primitives into app.js/app.css WITHOUT wiring them into any screen. Added: 6 icons (partners/orders/purchases/notes/consignOut/unit); the badge taxonomy on `.vgc-sm-tag` (roles filled, sourcing outlined, status semantic — helpers `badge`/`roleBadges`/`statusBadge`); unified summary `statTiles` (`.vgc-sm-tiles`/`.vgc-sm-tile`, hero=dark inverted, good/bad, mono values); `docHeader`; line cells `cellIn`/`cellRO`; and a slide-in `openDrawer`/`closeDrawer` (`.vgc-sm-drawerov`/`.vgc-sm-drawer`, closes on overlay/Escape/`[data-drawer-close]`, focus in+restore). **Collision averted:** `.vgc-sm-badge` was the nav dot, so pills use `.vgc-sm-tag`. Verified in a browser harness: all primitives render, taxonomy visually distinct, hero tile dark, drawer opens/focuses/closes 3 ways, console clean; live app unchanged (only --danger/ok/outline/product tag modifiers are used by screens today). Next: Phase 1 adopts these in the Order document. |
| 1.22.2 | **Fix: missing item columns — the real cause of "stuck as material / edits don't save".** The 1.21.0 `CREATE TABLE` carried inline `-- SQL comments`, which **dbDelta cannot parse**, so on existing installs `for_sale` (and likely `price_net`/`price_b2b`/`cost_manual`/`store_stocked`/`store_qty`) were never ADDed — reads returned empty (all items read as materials), writes to the column failed silently (role edits didn't persist). Removed all `--` comments from CREATE TABLE. Added idempotent `VGC_SM_Repository::ensure_item_columns()` (SHOW COLUMNS → ALTER ADD for each missing column) + `maybe_seed_for_sale()` (re-seeds only if nothing is for_sale yet, so it can't clobber edits). New bootstrap migration guarded by `vgc_sm_item_columns_v2`; DB_VERSION 0.20.1. Seed heuristic improved: now also marks `kind='manufactured'` as products (per user: most items are made-to-sell). Verified SQL generation against a stubbed `$wpdb` (all 6 missing columns ALTERed, correct seed UPDATE). **Lesson: never comment inside a CREATE TABLE dbDelta sees; the JS harness stubs the server so it cannot catch schema bugs.** |
| 1.22.1 | **Fix (1.21.0 bug, surfaced now): item read as both product AND material.** `tagHtml(kind)` still rendered the words "Product"/"Material", colliding with the new `for_sale` role badge — a made item seeded as a material showed both "Material" (role) and "Product" (kind), and a saved role change looked unsaved because the kind tag never moved. Relabelled the sourcing tag to **Made/Bought** (i18n `Made` added; `Bought` existed). The refactor (1.22.0) was **exonerated** first: a body-diff harness confirmed all 98 REST method bodies byte-identical, and a browser harness proved the role save round-trips (PUT sends `for_sale:1`, badge updates). Note: the 1.21.0 seed only marked sellable/priced items as products, so made-products with no price and not shop-sellable seeded as materials — users re-mark them individually (save works). |
| 1.22.0 | **Refactor (no behaviour change): `class-rest-api.php` split into resource traits.** The 3,212-line class became a ~620-line main class (route table `register_routes`, permission callbacks, `error_response`) that `use`s 9 traits under `includes/rest/` (`trait-items/production/partners/notes/orders/purchases/shop/reports/admin.php`), each holding one resource's handlers + `shape_*`. Traits are `require_once`d at the top of the main file. Safe because `self::`/`__CLASS__`/`array(__CLASS__,'x')` all resolve to the composed class regardless of the defining trait. **Verified:** the route-dump harness shows all 79 route-configs register byte-identically (same methods, callbacks, permission callbacks), and ReflectionClass confirms all 98 methods present, none extra, distributed 8 main + 90 across 9 traits. No DB change (0.20.0). To add an endpoint now: route in `register_routes()`, handler in the resource trait. |
| 1.21.0 | **Material/product divide: nav split + `for_sale` flag.** New items column `for_sale` (DB **0.20.0**), independent of `kind` (which stays bought/made and drives recipes/production). One-time `VGC_SM_Repository::seed_for_sale()` (guarded by option `vgc_sm_for_sale_seed`) sets for_sale=1 where `is_sellable=1 OR price_net IS NOT NULL OR price_b2b IS NOT NULL`. shape_item/item_input/sanitize_item + `list_items` gain `for_sale`; shop-pull and CSV import set it. Nav: `#/items` entry replaced by `#/products` + `#/materials` (new `beaker` icon); `#/items` kept as an unlisted all-view for back-compat; category subnav now hangs off `#/products`. `viewItems(scope)` (products/materials/all) drives title, the for_sale list filter, and a scope-aware New-product/New-material button (`#/item/new?for_sale=1|0`). Item form: a clear "This item is" role select (`f-for_sale`), the old Type relabelled "How it is obtained" (Bought in / Made from a recipe); item detail gains a Product/Material badge + a for_sale-aware back-link. **Bug caught in the browser harness before shipping:** the barcode-prefill wrapper `viewItemForm = function (id){ origItemForm(id) }` silently dropped the new `query` arg, so `?for_sale=` defaults failed — fixed to forward `(id, query)`. Verified end-to-end (nav, scoped lists, form defaults, edit) via a boot+fetch-stub harness; see memory [[verify-spa-ui-in-browser]]. |
| 1.20.1 | **Hotfix: item edit crashed with "i is not defined".** `bindPricing()` (a sibling helper defined OUTSIDE viewItemForm's `.then(function(r){ var i })` closure) referenced `i` via the new `currentCost()`. `node --check` passes (syntax-only) — the scope bug only shows at render. Fixed by threading the item in: `function bindPricing(i)` + call site `bindPricing(i)`. **Verified in the browser** via a static harness (DOM shell + `window.VGC_SM` boot + fetch stub, driven by javascript_tool): recipe and bought items both render, live margins recompute on price/cost/VAT/manual-toggle input, net↔gross convert. See memory [[verify-spa-ui-in-browser]] — node --check is not enough for app.js views. |
| 1.20.0 | **Three item prices + margins.** Items gain `price_b2b` (DB **0.19.0**; `price_net` already existed unused). Model: `cost_net` = manufacturing cost (recipe-computed or manual, from 1.19); `price_net` = retail/B2C; `price_b2b` = wholesale. Both prices stored **net**, gross derived from `vat_rate`. shape_item adds `price_net`/`price_gross`/`price_b2b`/`price_b2b_gross` and `retail_margin(_pct)`/`b2b_margin(_pct)` via a `$margin_of` closure: margin = price − cost, pct = margin/price×100 (ex-VAT both sides, % OF price — the app-wide convention). Null-safe when either side is unset. sanitize_item + item_input allow the two prices. UI: form Pricing section gains a Selling-prices block (net+gross pair each) with a **live margin line** (`wirePrice()` + `currentCost()`, which reads the recipe cost or the manual override); item detail hero relabelled "Manufacturing cost" and gains `priceMarginStrip()`. **i18n fix (root cause of the user's confusion):** `Unit cost*` was translated «Единична цена» = "unit *price*"; corrected to «Себестойност» (cost), matching Purchases' «Себест.». **No data migration** (user dismissed the choice): existing `cost_net` left as-is; if those values are really sale prices, a products-only `cost_net`→`price_net` copy is offered but not run. |
| 1.19.0 | **Physical-store allocation + manual cost override.** Items gain `cost_manual`, `store_stocked`, `store_qty` (DB **0.18.0**), plus a new `store_ledger` table. New `class-store.php` (`VGC_SM_Store`): `store_qty` is a cache, `store_ledger` the truth (SUM), same pattern as movements/stock. `distribute($id,$target)` moves units workshop↔store — writes ledger only, **total stock untouched** (guarded: target ≤ stock_qty). `sell($id,$qty)` writes ledger −qty AND a `store_sale` movement −qty, so a store sale is the one bucket move that also reduces stock. `clamp($id)` trims the bucket when it would exceed stock; called from `record_movement()` on any negative delta (soft dependency via `class_exists`), so a workshop ship/scrap auto-trims the store slice. REST: `POST /items/{id}/store-distribute {target}` and `/store-sell {qty}`. shape_item adds `store_stocked`/`store_qty`/`store_workshop`. **Cost:** `cost_computed = has_bom && !cost_manual`, so a recipe item can override its BOM cost by hand; shape adds `cost_manual`/`has_bom`/`cost_calc` (the BOM cost always, for the live form toggle). UI: item form gets a store checkbox + a manual-cost toggle that reveals `#f-cost-manual-block`; item detail gets a Physical store card (at store / at workshop stat tiles + Distribute/Sold-at-store, prompt()-driven like Publish). Verified with a stubbed DB: distribute holds total constant, sale drops both, guards + clamp hold, cache = ledger SUM. **Scope:** one store, allocation-bucket model (not full per-location movements); prices still never pushed to the shop. |
| 1.18.0 | **Partners are bidirectional; `partners.type` is dead.** The declared type gated the whole partner screen (`isOut`/`isIn`) and the Purchases vendor picker, so a shop we supply could not also supply us. Direction belongs to the document, not the partner — all three gates removed, the `pa-type` dropdown and `PARTNER_TYPES` deleted, and `save` no longer sends `type`. The **column and its sanitiser remain** (no migration; existing rows keep `customer`) but nothing reads them — `VGC_SM_Partners::all()`'s `type` filter was already dead code (no caller ever passed it). Replaced by **observed** roles: new `VGC_SM_Partners::roles( $id )` → `{customer, supplier}` via short-circuiting EXISTS queries over orders/purchases with a notes(direction) fallback (2 queries when both hit, 4 worst case); surfaced as `is_customer`/`is_supplier` on `shape_partner`, rendered by `partnerRole()`. `shape_partner[we_owe]` now adds `VGC_SM_Purchases::balance_owed()` — it counted only notes before, so purchase debt was invisible on the partner page. `get_partner` returns `purchases`; the partner screen gained a Purchases card, a New purchase action and `[data-purchase]` click-through. **Cost note:** the partners list now runs roles() + two balance_owed() loops per partner; fine at their scale, worth batching if partner counts grow. |
| 1.17.0 | **Purchases — the inbound mirror of Orders.** New `class-purchases.php` + tables `vgc_sm_purchases` / `_purchase_lines` / `_purchase_payments` (DB **0.17.0**); items gain `price_net`. Same delta-reconcile contract as orders: `applied_in` / `applied_held` are what the ledgers hold, `reconcile()` writes only the difference. **mode `buy`** → `record_movement('receive')`, stock up, owe received−returned. **mode `consignment`** → writes `consignment_ledger` direction **`in`** (the existing held bucket), so `VGC_SM_Notes::held()`, the Held screen and shop publishing pick it up unchanged; owe only `qty_sold`. Flipping mode consignment→buy needs no special action — the next reconcile emits the delta (held→0, stock→received−returned). `billable_qty()` mirrors orders' full/on_sale. **VAT:** `vendor_vat_registered` is snapshotted per document so editing the partner cannot restate a settled purchase; an unregistered vendor's lines are forced to `vat_rate = 0`, which makes the shared `strip_vat()` a no-op — their price is final. **Markup, not margin** (user's explicit choice): `sell_net = net_cost × (1+markup/100)`; `sell_gross` uses the ITEM's vat_rate (our sale VAT), never the vendor's; `margin_from_markup()` exists so the UI shows both (20% markup = 16.7% margin) and the number Orders reports later is never a surprise. Verified: 10.00 final from an unregistered vendor and 12.00 gross from a registered one both give cost 10.00 and shelf 14.40. UI: `viewPurchases`/`viewPurchase`, `.vgc-sm-pline` grid (mirrors `.vgc-sm-oline`), inline New-item panel (no modal system exists). Prices are never pushed to the shop — deliberate; most of this stock sells in the physical location. |
| 1.16.1 | **Stat row fixed — a class-name collision, not a layout tweak.** `.vgc-sm-stat` was already defined (app.css ~line 398) by a replaced component as `display:flex; align-items:flex-end; justify-content:space-between; flex-wrap:wrap`, with `> span` / `strong` children. The 1.15.0 tiles reused the name and never declared `display`, so the older rule won at equal specificity and every tile rendered as a flex row with the figure and caption pushed apart. The old block was **dead** (no markup referenced it) and is deleted. Tiles are now built by `stat(label, value, mod)` / `qual()` / `statRow(tiles)` helpers: caption div first, value div second. `statRow()` filters nulls, computes `--stat-cols = ceil(n/2)` and pads with one blank `.vgc-sm-stat` on an odd count (the divider background showed through the empty cell). CSS: base is `repeat(2, minmax(0,1fr))`; `@media (min-width:900px)` uses `repeat(var(--stat-cols,5), minmax(0,1fr))` — auto-fit cannot know a count that changes with the data, and strung all nine tiles across one line. |
| 1.16.0 | **`prices_include_vat` now governs the whole row, cost included.** It only ever reached `unit_price`, so a gross-typed `unit_cost` was subtracted at full value from a VAT-stripped price — every margin on a gross order was understated by the VAT on cost. New private `VGC_SM_Orders::strip_vat( $amount, $line, $incl )` is the single strip; `net_unit()` and the new public `net_cost()` both delegate to it, so the two can no longer diverge. `totals()` uses `net_cost()`; so does `line_margin` in the REST order payload (class-rest-api.php, which had its own raw copy of the formula). `save_lines()` takes `$incl` and **grosses up the `unit_cost_net()` default** when the order is gross-typed — the BOM computes net, so an untouched default would otherwise be stripped of VAT it never had. UI: `vatBasis()` appends a live «с ДДС»/«без ДДС» marker (`.vgc-sm-oline__basis`) to the Price and Cost headers. **Migration risk:** costs stored before this on `prices_include_vat=1` orders were net (BOM autofill) or gross (hand-typed) — indistinguishable in the data, so no migration was written; hand-typed ones become correct, autofilled ones now under-count. |
| 1.15.1 | **Margin % fix + VAT semantics on the statement.** `statement()[totals][margin_pct]` divided margin (ex-VAT on both sides) by **gross** turnover while `VGC_SM_Orders::totals()` divides by **net** — the same order reported two different margins. `statement()` now accumulates `turnover` (**net**), `vat` and `billed` (**gross**) separately; `margin_pct` = margin / **net** turnover, matching the order screen. `avg_doc` is gross (it pairs with the Total column). Statement tiles: Turnover carries an "excl. VAT" qualifier (`.vgc-sm-stat__q`), plus new Billed (incl. VAT) and VAT (shown only when non-zero) tiles. i18n: added `Quantity`/`Unit price`/`SKU`, which were rendering in English. |
| 1.15.0 | Statement stats: `statement()[totals]` gains `turnover`/`paid`/`margin`/`margin_pct`/`items`/`orders`/`avg_doc` (margin only from orders — notes have no cost snapshot; item counts come from one grouped query over note lines + order billable qty). UI is a **KPI row of stat tiles** (`.vgc-sm-statrow`), not a chart: values wear ink tokens, colour is spent only on state (overdue) or signed direction (profit) and never without its label. Dropped `tabular-nums` from `.vgc-sm-kpi__v` — tabular is for columns, not display-size standalone values. |
| 1.14.0 | `VGC_SM_Notes::statement()` now merges **orders and notes into one timeline** (it only read notes before, so order money was missing entirely). Every statement line carries `total`/`paid`/`balance` + `kind` (note\|order): a note is all-or-nothing (issued = owed in full, settled = paid in full); an order has a real payment trail. Completed orders contribute 0 outstanding, like settled notes. Orders list, partner orders card and the statement all show Total + Paid columns. |
| 1.13.0 | **Partial payments** (`order_payments`, DB 0.16.0) — `add_payment()`/`delete_payment()`/`paid_total()`; `totals()[paid]` is the sum of the trail. **Overdue fix**: `overdue` now also requires `balance > 0.005` (a paid order past its date was wrongly flagging). Orders surfaced on the partner screen (`get_partner` returns `orders`), and `shape_partner[owed]` = notes balance **+** open-order balances. |
| 1.12.0 | Display pieces moved to their own section in the order editor (no price/VAT/margin fields; excluded from totals via `billable_qty()`), and `orders.prices_include_vat` (DB 0.15.0) lets prices be typed VAT-inclusive — `VGC_SM_Orders::net_unit()` derives the net, the typed figure is stored untouched. |
| 1.11.0 | **Orders** — a mutable, order-centric model replacing the note-per-event dance for customer orders. `class-orders.php`, `orders`/`order_lines` (DB 0.14.0). Ordered/sent/sold/returned per line on one screen; payment mode `full`\|`on_sale`; transport + payer; paid/balance; **margin** from a snapshotted `unit_cost`; complete/reopen. Editing reconciles the ledgers by delta (`applied_out`/`applied_pile`). Stock notes remain for ad-hoc/inbound movements and share the same ledgers. |
| **1.10.0** | **Payment terms, refundable returns, display flag, print fixes** (DB 0.13.0). `partners.payment_terms_days` → `stock_notes.due_date` on issue (only for `is_money_note()` types; credits never fall due); statement gains due/overdue + an `overdue` total. `stock_notes.is_refund` on a `return_in`: credits `balance_owed`, skips the consignment write **and** its pre-flight (those goods were billed, not lent). `is_display` on note lines **and** the consignment ledger splits the pile into sellable/display pools — `outstanding()` takes a `$display` filter, sale reports can only draw on the sellable pool, `outstanding_all()` returns `display_qty`. Print: `@page A4 portrait`, cards `break-inside:auto` (they *must* flow), `thead` repeats, rows never split, `.vgc-sm-tablewrap` overflow visible (it was clipping), thumbs hidden. |
| 1.9.0 | **Note dates, "already sent", destinations, per-line VAT + notes, partner VAT** (DB 0.12.0). `note_date` drives `issued_at` on issue; `skip_stock` makes `issue()`/`cancel()` bypass the stock ledger *and* its pre-flight (consignment still applies); `location_id` = destination (validated against the partner's own locations). Lines gain `vat_rate`/`line_vat`/`line_gross`/`line_note`; net↔gross auto-fill is client-side, net is what's stored. **Balances + statements now sum `total_gross`** — one-time `backfill_vat()` sets legacy notes to gross=net/0% so they still read correctly. Partner `vat_registered`/`vat_number`. Editor lines are one flex row (`.vgc-sm-nline`). `list_partners` now returns `locations` via `locations_map()` (one query). |
| 1.8.0 | **Pull filters + virtual default** (needs Bridge 0.4.0): category (`product_cat` slug) and product-type filters on the pull screen, populated from `/shop/product-categories`. Bridge `shape_product` adds `virtual`/`downloadable`; `list_products` takes `category`/`type`. Auto-tick now skips `virtual` products (shown with a tag) as well as `already`. |
| 1.7.1 | **Settings hub**: the four admin config screens (Connection=`/settings`, `/translations`, `/team`, `/audit`) are grouped under one **Settings** sidebar entry with a shared tab bar (`settingsTabs()` + `SETTINGS_TABS`); `activeKey()` maps all four to `#/settings`. Help moved to its own bottom navlist (`NAV_HELP`). UI-only, no schema change. |
| **1.7.0** | **Roles + audit log**: four access levels (viewer/operator/manager/admin) via `VGC_SM_Access::level()`/`at_least()`/`set_level()` (user-meta `vgc_sm_level`); every REST route gated by `$write`/`$mgr`/`$admin`; boot `perms` + level drive UI gating. `class-audit.php` (`VGC_SM_Audit::log/query`) + `audit` table; logged at the mutation handlers and on `wp_login`/`wp_logout`. New admin screens **Team** (`#/team`) and **Activity log** (`#/audit`); DB_VERSION 0.11.0. |

---

## 9. Consignment: what is agreed but not yet built

All five phases have shipped: outbound (1.1.0), inbound/held bucket (1.2.0), multi-location/contact partners (1.3.0), shop publishing of held stock (1.4.0), and partner statements + printable notes (1.5.0). The consignment feature set is complete.

Standing defaults the user accepted: **held stock is consumed oldest-received-first** across makers when reconcile books shop sales (FIFO); **the shop never offers more held units than we hold** (`clamp_shop_held`); own-stock online sales remain out of scope (push-only design — the shop is authoritative for its own sales, we don't pull orders back). **You can only release what you own** (a maker's held goods cannot be released onward on an outbound note).

---

## 10. Known gaps / candidate next steps

- Historical costs are **not snapshotted** at run time — reports estimate using each item's *current* unit cost.
- Pulling many products with photos does one download per product; a huge catalogue can hit a PHP timeout (pull in batches, or move image copying to a background job).
- No margin view (computed cost vs shop selling price) — the Bridge already returns `price`, so this is cheap to add.
- No order pull-back: online sales reduce shop stock but not local stock. Deliberate (the user chose push-only), but a `/orders` style sync is the natural extension if local and shop stock must fully agree.
- Categories are a **flat list** (no nesting).
- Archiving a sellable item does **not** touch the shop (the WooCommerce product stays live).
- The wiki (`class-help.php`) is **English only** — its body HTML isn't routed through the i18n catalogue.
